# 설계 고정 문서 — 반복된 오독을 막기 위한 정본

> ## ⛔ 연구 종료 (2026-08-22) — 최종 판정은 `FINAL_REPORT.md`
> 설계 부품 ①청산맵 ②오더북 깊이 ③유입·취소 는 **셋 다 기여 0** 으로 확정됐다.
> 남은 것은 "15분에 3% 넘게 떨어지면 산다" 하나이고, 그 우위조차 호가창보다 작다.
> 이 문서는 **왜 그렇게 판정됐는지의 근거**로 계속 유효하다 — 특히 §3(오독 11개).

작성 2026-08-04. **설계 해석에 관해서는 이 문서가 최상위다.**
`ARCHITECTURE.md`·`STATUS.md`·`MODEL.md`와 충돌하면 **이 문서가 맞다.**
단 **성적·판정 수치는 `FINAL_REPORT.md` 가 우선**한다.

## 왜 이 문서가 필요한가

같은 오해가 여러 번 반복됐다. 전부 **내가** 만든 것이고, 사용자가 매번 잡아줬다.
그래서 오해 자체를 여기 박아둔다. **다음에 읽는 사람(=미래의 나)은 §3을 먼저 읽어라.**

---

# 1. 설계 원문 — 사용자의 말. 바꾸지 말 것

> **현재가 기준 상승·하락 양방향으로 격리 OI가 여러 가격대에 걸쳐 쌓여 있다.
> 히트맵으로 그 OI 깊이를 미리 파악해 두고, 가격이 한쪽으로 움직이면 그에 따라
> 오더북도 변한다. 그 변화를 매 순간 다시 읽어, 그 격리 OI가 강제청산될 때
> 가격이 얼마나 밀릴지를 계속 재계산한다. 그렇게 캐스케이드가 바닥을 칠 가격대를
> 확률적으로 근사하고 그 자리에 지정매수를 걸어둔다.**

## 1.1 설계는 **세 부품의 합**이다. 하나씩 떼면 설계가 아니다

$$\boxed{\;\hat X \;=\; \inf\Big\{\,u \;:\; \underbrace{\int_0^u \big[D(v,t) + \Delta(v,t)\big]\,dv}_{\text{② 오더북 + ③ 유입·취소}} \;\;\ge\;\; \underbrace{\int_0^u L\big(p_t(1-v)\big)\,dv \cdot \phi}_{\text{① 지도에서 나올 물량}}\Big\}\;}$$

| 부품 | 기호 | 역할 | 데이터 |
|---|---|---|---|
| **① 지도** | $L(p)$ | 가격대별로 **나올 물량** (분자) | 6년 OI로 재구성 / HL 직접 |
| **② 오더북** | $D(v,t)$ | 그 물량을 **받아낼 깊이** (분모) | `book_depth` 30초 / `depth_ws` 1초 |
| **③ 유입·취소** | $\Delta(v,t)$ | 깊이의 **시간 변화** ("매 순간 다시 읽어") | `depth_ws_flow` 1초 |
| 전환율 | $\phi$ | 지도상 물량 중 실제로 나오는 비율 | 추정 대상 |

**확률성의 출처**: $D$는 확정 물량이 아니다. MM이 넣었다 뺐다 한다.
실측 잔존율 $1/W$ 중앙 **3.4배** / p90 **17배**. 그래서 $\hat X$는 점이 아니라 **분포**다.

$$P(X \ge u \mid \mathcal F_t)$$

를 만들고, 그 분위수에 **지정매수**를 건다. 완벽히 맞출 필요 없다. **근사면 된다.**

---

# 2. 세 가지 양을 절대 섞지 말 것 ★ 이 세션에서 두 번 섞었다 ★

지도는 세 항의 곱이고, **셋의 성질이 전부 다르다.**

$$L(p,t) \;=\; \sum_{\tau<t} \underbrace{\Delta \mathrm{OI}^+(\tau)}_{\text{언제 얼마나 열렸나}}\cdot
\underbrace{f_R\big(p/p(\tau)\big)}_{\text{어디서 강제청산되나}}\cdot
\underbrace{S(t-\tau)}_{\text{아직 살아있나}}$$

| 항 | 무엇을 결정하나 | **자발 행동이 관여?** | 출처 |
|---|---|---|---|
| $f_R$ | **강제청산 위치** | **아니오 — 레버리지가 정한다** | HL 격리 포지션 실측 청산가 |
| $S$ | 그 포지션의 **생존** | **예 — 존버·물타기·익절** | 집계 OI 이탈률 $h(x)$ |
| $\Delta\mathrm{OI}^+$ | 진입 시점·가격 | — | 5분 OI 증가분 |

## 2.1 ❌ 내가 저지른 혼동 — 반복 금지

**강제청산은 선택이 아니다.** 담보가 부족하면 청산가에서 **자동으로** 나간다.
거기에는 "손해 보면 존버한다" 같은 행동 함수가 **없다.**

그런데 나는 집계 OI 이탈률 $h(x)$ 를 재고 그것을 **"지도의 커널"** 이라 불렀다.
$h(x)$ 는 OI 감소에서 나오고 **그 감소의 약 99%는 자발적 청산**이다
(청산 프린트는 1%). 즉 **$h(x)$ 는 $f_R$ 이 아니라 $S$ 다.**

> **$h(x)$ 를 강제청산 위치로 읽지 말 것.** 그것은 개인의 행동이지 청산 규칙이 아니다.

## 2.2 방향을 바로잡으면 결과가 뒤집힌다

D-1 실측: **손실 쪽 이탈률이 이익 쪽의 0.67배** ($t$=−3.5, EM 6회 수렴).
이것을 $S$ 로 읽으면:

> **손실 포지션은 자발적으로 안 나가고 남는다. 그러므로 가격이 그 청산가까지
> 내려갔을 때, 일정 이탈률을 가정한 모형보다 연료가 더 많이 쌓여 있다.**

**설계를 부정하는 것이 아니라 강화한다.** 나는 이것을 정반대로 읽어 "설계 전제와
반대" 라고 보고했다. 철회한다.

$x<-30\%$ 에서 $h$ 가 **4.04배**($t$=8.6)로 튀는 것도 자발 행동이 아니라
**거기서 실제 강제청산이 터진 흔적**일 가능성이 높다(레버리지 3~5배 청산가 구간).
즉 $h$ 안에 $f_R$ 이 섞여 들어왔다. **분리해야 한다.**

## 2.3 그럼 "총 디레버리징" 은 어디에 쓰나 — 실현 물량 $V$ 에만

| 양 | 정의 | 근거 |
|---|---|---|
| **지도 $L(p)$** | **강제청산분만.** 그것만이 가격으로 **미리 계산 가능**하다 | 자발 청산은 발생 가격이 정해져 있지 않다 |
| **실현 물량 $V$** | **총 디레버리징** (강제 + 뒤따르는 자발) | 호가를 먹는 데는 강제·자발 차이가 없다 |

**강제청산이 방아쇠, 자발이 뒤따르는 물량이다.** 지도는 방아쇠를 그리는 것이고,
캐스케이드 크기는 뒤따르는 물량까지 포함한다.

이 구분을 뒷받침하는 실측:

| 증거 | 수치 | 함의 |
|---|---|---|
| 청산 프린트 / OI 감소 | **약 1%** | 실현 물량은 대부분 자발분 |
| **프린트만으로 방아쇠** | $t$ = **−0.1** | 프린트는 **크기를 못 재서** 방아쇠로 무용 |
| **OI 급감으로 방아쇠** | **+48.3bp** ($t$=2.8) | 총 디레버리징 **규모**가 정보다 |

> 프린트가 무용한 이유는 "강제분이 중요하지 않아서" 가 아니라
> **1% 표본이라 규모를 못 재기 때문**이다. 두 진술을 혼동하지 말 것.

## 2.4 격리 vs 교차 — 여전히 나눠야 한다
격리는 청산가가 **포지션 단독으로** 결정돼 계산 가능하다. 교차마진은 계좌 전체
담보에 의존해 외부에서 계산 불가다. **$f_R$ 은 격리분에서만 추정할 수 있다.**
교차분은 $S$ 나 잔차로 흡수시킨다. **미검정.**

---

# 3. 오독 방지 — 다음에 이렇게 읽지 말 것

## 3.1 ❌ "A-2가 실패했으니 지도가 실패했다"

**A-2가 실제로 잰 것**: 지도 **단독**으로 $X$를 예보할 수 있는가 → 증분 0.

**A-2가 말하지 않는 것**:
- 지도가 쓸모없다 (X)
- 설계가 실패했다 (X)

> **지도는 분자만 준다.** 분모(②③) 없이 $X$가 나올 리가 없다.
> **지도만으로 바닥을 못 맞힌 것은 실패가 아니라 당연한 결과다.**

**지도의 세 용도를 구분할 것:**

| 용도 | 검정 | 결과 |
|---|---|---|
| 청산 **위치** 맞히기 | A-1 | **약한 통과** ($t$=3.6~4.1, 증분 $R^2$ +0.044) |
| 단독으로 **바닥** 예보 | A-2 | **실패** — 당연하다. 설계가 요구한 적 없다 |
| **방아쇠**로 쓰기 | — | **미검정** |
| **분자**로 ②③과 합치기 | — | **미검정. 이것이 설계다** |

## 3.2 ❌ "$V/D$가 **기각**됐다" — 기각이 아니라 **명세 오류**였다

> **2026-08-04 정정.** 앞서 "$V/D$ 기각" 으로 기록했다. **철회한다.**
> 명세 결함이 최소 4개 있었고, 그 상태의 귀무 결과는 기각 근거가 못 된다.

| # | 결함 | 왜 치명적인가 |
|---|---|---|
| **1** | **흐름 vs 재고를 비교했다** | $Q$ 는 5분 **누적 감소**(흐름), $D$ 는 **한 순간 깊이**(재고). 그 5분간 호가는 계속 채워진다. 올바른 분모는 $D+\int\Delta$ 인데 **$\Delta$ 를 한 번도 안 넣었다** |
| **2** | $X$ 를 **240분** 최대역행으로 쟀다 | $V/D$ 가 예측하는 건 호가를 먹는 **순간의 변위**다. 4시간 창은 무관한 움직임이 잡음으로 들어간다 |
| **3** | **로그-로그**만 적합했다 | 임계형 $\max(0,V/D-c)^\beta$ 라면 로그-로그는 **구조적으로 못 본다** |
| **4** | 해상도가 5분/30초였다 | 업계 표준 임팩트 모형은 전부 **틱~초** 단위로 캘리브레이션한다 |

**정확한 진술**: "$Q$=실현 OI감소, $D$=직전 재고, $X$=240분, 로그-로그, 5분 해상도
라는 명세에서는 $b_2$ 가 유의하지 않았다"(0.05~0.11).
**설계의 $V/D$ 는 검정된 적이 없다** — 설계의 $V$ 는 지도가 주는 **예상 물량**이고
분모에는 $\Delta$ 가 들어간다.

→ 재검정: `analysis/book_prob.py` (웹소켓 1초, $D+\Delta$, 15/60/300초, 임계형 포함)

## 3.2b ❌ "자기여기 증폭이 **기각**됐다" — 이것도 명세 오류다

> **2026-08-04 정정.** R-4 에서 $A$=1.44 ($n$=0.307) 를 근거로 "증폭 기각" 이라 적었다.
> **철회한다.**

| # | 결함 |
|---|---|
| **1** | **$n$ 을 5분봉 OI 에서 쟀다.** 문헌의 $n\approx0.8$ 은 **밀리초 체결 타임스탬프**에서 나온다. **같은 대상이 아니다** |
| **2** | **33%의 이벤트가 1봉에서 끝났다.** 5분 해상도에서는 봉 안의 분기를 **원리적으로 볼 수 없다.** 진짜 캐스케이드는 초 단위다 |
| **3** | 되돌림이 $A$ 에 음(−)인 것은 **타이밍 인공물**이다. $A$ 가 크다 = 캐스케이드가 여러 봉 계속됐다 = 진입이 **초반**이었다 = 결과가 나쁘다. 증폭의 반증이 아니다 |

**정확한 진술**: "5분봉 OI 에서 다봉 연속성이 크지 않다"($A$=1.44).
**밀리초 청산 프린트에서 잰 $n$=0.801 은 문헌값과 정합했고, 그것이 기각된 적은 없다.**
(잔차 진단 불합격은 별개 문제 — 주문 분할 때문일 가능성. 분할 병합 후 재검정 필요.)

## 3.2c ❌ 업계가 안 쓰는 모델이라서 실패한 게 아니다

설계가 쓰려는 모델 — Hawkes 자기여기(Bacry-Muzy, CFM/Bouchaud), Queue-reactive
(Huang-Lehalle-Rosenbaum), Propagator, 제곱근 법칙 — 은 **전부 업계 표준**이다.
**문제는 모델이 아니라 내 데이터 해상도였다.** 이것들은 전부 **틱~초** 단위로
캘리브레이션한다. 5분봉으로 재고 "안 된다" 고 한 것이 오류다.
**웹소켓 1초가 처음으로 올바른 해상도다.**

## 3.3 ❌ "청산 프린트로 청산을 세면 된다"
프린트는 OI 감소의 **약 1%**만 태그된 표본이다. **크기를 못 잰다.** §2.1 참조.

## 3.4 ❌ "부품 검정이 실패했으니 전략이 실패했다"
부품이 각각 죽어도 조립된 전략은 돌 수 있고 반대도 마찬가지다.
**부품 검정 15개를 돌린 뒤에야 전략 백테스트를 한 번 돌렸다.** 순서가 틀렸다.

## 3.5 ❌ "48.3bp 백테스트가 설계다"
**아니다.** 그것은 히트맵 없음 · 오더북 재계산 없음 · 확률 추정 없음 · **시장가**다.
설계 부품이 하나씩 떨어지는 동안 남은 **잔해**이지 설계의 구현이 아니다.
넘어야 할 **기준선**으로만 쓸 것.

## 3.5b ❌ "사건 순간 진입이 −229.6bp 이니 그 시점은 나쁘다"

**반대다.** 그 시점 이후로 **X 중앙 425bp 가 더 밀린다**($X/r_0$=5.30).
역방향(되돌림)으로 들어가서 손해인 것이고, **순방향(슈팅)이면 그게 수익**이다.
되돌림 51bp 의 8배다. 되돌림은 **바닥을 맞혀야** 하지만 슈팅은 **시작만** 맞히면 된다.

> ### 단 그 +215.5bp 는 **생존자 편향**이었다 (2026-08-04 실측)
>
> | 판 | 사건 | 슈팅 추적150bp | $t$ |
> |---|---|---|---|
> | 룩어헤드(5분봉 8σ 안에서 3σ 봉 선택) | 1,005 | **+215.5** | 9.6 |
> | **실시간 OI (지연 0)** | **3,534** | **−2.0** | **−0.4** |
> | 가격만 | 406,568 | −10.8 | −22.3 |
>
> **"3σ로 시작해서 결국 8σ까지 간 경우" 만 남긴 것**이었다. 흐지부지된 2,500건이
> 통째로 빠졌다. 방향도 5분봉 전체 부호에서 가져왔다(실시간엔 모른다).
>
> **교훈**: 5분봉 사건 정의 **안에서** 1분봉을 고르면 자동으로 생존자 편향이 된다.
> 사건 정의와 진입 시점의 **정보 집합이 같아야 한다.**

## 3.6 ❌ "집계 OI 이탈률 = 강제청산 커널"

**두 번 섞었다.** 자세히는 §2.1. 요약:
- 강제청산은 **선택이 아니다.** 행동 함수가 없다
- 집계 OI 감소의 **99%가 자발분**이다 → $h(x)$ 는 $S$ 이지 $f_R$ 이 아니다
- $f_R$ 은 **레버리지 분포**에서 나온다 (HL 격리 포지션의 실측 청산가)

**증상**: "손해 볼수록 덜 판다 → 설계 전제와 반대" 라고 보고했다.
**정정**: $S$ 로 읽으면 **손실 포지션이 남아 연료가 쌓인다 → 설계를 강화한다.**

## 3.7 ❌ 내가 반복한 통계 오류 3종

| 오류 | 증상 | 교정 |
|---|---|---|
| iid 표준오차 | $t$가 **17.4배** 부풀음 ($\ell$=1에서도) | **일클러스터 CR1 필수** |
| 심볼 고정효과 누락 | 횡단면 효과를 시계열 효과로 오독 ($t$ 3.1→1.4) | **FE 넣고 재확인** |
| $R^2$로 임팩트 판정 | "청산은 못 민다" 결론이 여기서 나옴 | **조건부 평균 / 캘리브레이션** |
| **점추정에 서사 붙이기** | "매도×하락장이 주범"(n=95, $t$=−0.9) | **$t$ 를 먼저 보고 말한다** |
| **부분집합만 집계** | 밀림레그 +93.9bp — 체결된 건만 셌다 | **시도당(미체결=0)으로 낸다** |
| **격자 최고 칸 보고** | 선택 편향이 샤프를 부풀림 | **전/후반 분할이 둘 다 서야 한다** |

## 3.8 ❌ "바닥을 잘 맞히면 돈이 된다"

**아니다.** 동적 재호가로 체결후 MAE 를 −201.9 → −71.7 bp 로 **2.8배 개선**했는데
시도당 수익은 41.4 → 13.9 bp 로 **3배 나빠졌다** (§5.13).

수익은 **가격 이탈의 크기**에서 나온다. 작은 저점에서 정확히 바닥을 잡아봐야
이후 상승이 중앙 15.9bp 라 수수료 7bp 를 빼면 남는 게 없다.

> **"바닥 예측 정확도" 를 목표 함수로 삼지 말 것. 이탈 크기로 선별하는 것이 레버다.**

## 3.9 ❌ "지정가로 바닥에 걸면 된다" — 설계 원문이지만 원리상 불가

$p_{lim}=p_0(1-q_\alpha)$ 의 **체결 조건이 곧 $X\ge q_\alpha$** 다.
체결되는 순간은 언제나 "바닥이 내 주문보다 아래" 인 경우다.
게다가 $m(u)=E[X-u\mid X\ge u]$ 가 $u$ 에 **단조 증가**한다(파레토 꼬리) —
**깊이 걸수록 남은 밀림이 커진다.** 어떤 $\alpha$ 를 골라도 안 바뀐다 (§5.12).

> 설계 원문의 "그 자리에 지정매수를 걸어둔다" 는 **정적 주문으로는 실현 불가능하다.**
> 남은 길은 (a) 시장가 + 강한 선별(§5.15, 현재 최선) 또는
> (b) 호가창으로 "지금 멈췄다" 를 실시간 탐지 — 후자는 표본이 없어 미검정(§5.16).

## 3.10 ❌ "회전율을 올리면 HFT 처럼 된다"

**안 된다.** 우위가 **희소성에서 나오기 때문에** 건수를 늘리면 우위가 더 빨리 사라진다:
13배 늘리면 39배 줄어든다. 동시보유가 1 을 넘는 영역은 우위가 이미 0 이다 (§5.15).

**단 방향을 반대로 하면 열린다** — 더 극단적인 사건만 골라도 **연간 총량이 오히려
커진다**(8,389 → 18,953). 그리고 **1분봉으로 옮기면 건수와 우위가 동시에 오른다**
(연 174 → 361건, ~~샤프 1.16 → 2.38~~). 회전율의 레버는 방아쇠 **강도**와 **해상도**다.

> **[2026-08-05 정정]** 위의 "샤프 2.38" 은 **손절 모형 인공물**이다 (§3.11, §5.17).
> 정정 후 같은 방아쇠의 샤프는 익절8σ·상한30분 기준 **1.74**(gap=1)·**1.26**(gap=5).
> 방향(1분봉이 5분봉보다 낫다)은 유지되나 **배수는 이 값을 쓸 것.**

## 3.11 ❌ "진입 봉은 청산 검사에서 건너뛴다" — 시장가 진입에서는 **틀렸다**

**지정가 진입에서는 맞다.** 봉 안 어디에서 체결됐는지 모르므로, 체결을 만든 바로 그
봉의 저가·고가로 청산을 판정하면 룩어헤드다. `prob_entry.py` 가 `lo = fj + 1` 인
이유이고 그것은 옳다.

**시장가로 봉 시가에 들어가면 그 봉 전체가 진입 이후다.** 건너뛰면 두 가지를
동시에 가정하게 된다:

1. 진입 후 **60초 동안 손절이 꺼져 있다** — 실거래에서 진입과 동시에 스톱을 걸면
   그 1분이 가장 위험한 구간이다
2. 그다음 봉에서 **이미 시장이 지나가 버린 손절가에 체결된다**

실측 (`fast_trigger.py` 의 §6.17 설정, 1,721건, 2020-09-02~2026-07-29):

| | 평균 | SE | $t$ |
|---|---|---|---|
| 진입봉 건너뜀 (원본) | +30.4bp | 6.80 | **4.5** |
| 진입봉부터 검사 (정정) | **−2.6bp** | 4.80 | **−0.5** |

- 진입 1분 안에 손절선을 뚫는 거래 **817건 = 47.5%**. 원본은 그중 **164건을
  평균 +224bp 익절**로 처리한다.
- 두 모형 총합 차이(−56,906bp)의 **100.9%** 가 이 817건에서 나온다.
- 별개로 원본의 손절 962건 중 **367건(38.1%)** 은 손절 봉의 **시가가 이미 손절가
  아래**다. 그것만 고쳐도 −4.0bp.

**같은 결함이 있는 곳**: `fast_trigger.py:132`, `asymmetry.py:99`.
**영향 없는 곳**: 익절·손절을 안 쓰는 모든 "시간청산만" 설정 (기준선 포함).

---

# 4. 설계 구현 순서 — 이 순서를 벗어나지 말 것

| 단계 | 내용 | 데이터 | 상태 |
|---|---|---|---|
| **D-1a** | $S$ — 생존함수. 집계 OI 이탈률 $h(x)$ | 6년 5분 OI | **완료** (§2.2, §5.1) |
| **D-1b** | $f_R$ — **강제청산 위치**. 레버리지 분포에서 | HL 격리 포지션 | **완료** (§5.2) |
| **D-2** | $L$ 조립 + $X$ 예측 검정 | 위 둘 + 6년 | **완료** (§5.4) — 캐스케이드 구간만 약한 양수 |
| **D-3a** | ② 깊이 · ③ 유입·취소 (지도 없이) | 웹소켓 1초 | **완료 · 작동** (§5.5) |
| **D-3b** | ①②③ 전부 결합 | 웹소켓 + 지도 | **완료 · 지도 기여 0** (§5.6) |
| **D-4** | $\Delta$ 프레임 보정 → 취소압 재판정 | 웹소켓 1초 | **완료** (§5.8) — 취소압 여전히 미검출, 회복력 강화 |
| **D-5** | 지정가 버전 평가 (설계 원문의 실행 방식) | 6년 1분봉 | **완료** (§5.9~5.13) — **정적은 원리상 불가**(§5.12) |
| **D-6** | 기준선을 이기는 설정 찾기 | 위 전부 | **완료 · 수치 정정** (§5.15, §5.17) — 연간총 8,389 → **17,662**, 샤프 1.16 → **1.74** |
| **D-7** | 오더북 특징 결합 | `book_depth` 30초 3.5년 | **완료 · 증분 0** (§5.16) |
| **D-10** | 최선 설정의 견고성 | 위 전부 | **완료** (§5.17) — **손절 모형 결함 발견. §6.17 수치 무효** |
| **D-8** | 밀리초 Hawkes 재검정 (주문 분할 병합) | `bybit_liq` | **미착수** |
| **D-9** | 웹소켓에 캐스케이드가 쌓이면 ②③ 재판정 | `depth_ws` 1초 | **대기** (표본 부족) |
| **D-11** | **보유 지평에 내부 최적이 없다** — 240분까지 단조 증가 | 6년 1분봉 | **미해결** (§5.17.6) |

**합격 기준**: $R^2$가 아니라 **캘리브레이션**(Kupiec / PIT / Christoffersen / 핀볼)
**+ 위약 대조 + 일클러스터 CR1 + 전/후반 분할**. 그리고 **기준선을 이겨야 한다.**

## 4.1 지금 하지 말아야 할 것
- 부품을 하나씩 더 검정하는 것
- **정적 지정가를 다시 시도하는 것** — §5.12 에서 원리상 불가가 확정됐다
- **밀림(슈팅) 레그를 다시 시도하는 것** — §5.11, 16개 조합 전부 손실
- **회전율을 올려 HFT 영역으로 가려는 것** — §5.15(1), 우위와 정면 상충
- 격자에서 최고 칸을 고르고 **전/후반 분할 없이** 보고하는 것
- **손절을 다시 붙이는 것** — §5.17.2, 터치·종가 어떤 폭에서도 음수
- **시장가 진입에서 진입 봉을 청산 검사에서 빼는 것** — §3.11

## 4.2 다음에 할 일 (우선순위)
1. **D-11 보유 지평** — 240분까지 꺾이지 않는다. 15분·60분 프레임 자체를 버려야
   하는지, 아니면 자본·용량 제약이 상한을 정하는지 (§5.17.6)
2. **용량 재측정** — 우위가 알트에 있고(38.4 vs 26.6bp) 진입·청산이 둘 다 시장가다.
   `CAPACITY_FINDINGS.md` 의 중앙 $373K 는 **지정가 진입** 기준이라 그대로 못 쓴다
3. **D-8 밀리초 Hawkes** — 5분봉 해상도로 잰 기각을 철회했으므로 재검정 필요
4. **D-9 대기** — 웹소켓에 캐스케이드가 담기면 ②③ 을 1초 해상도로 재판정

---

# 5. D-1 결과 (2026-08-04)

## 5.1 $S$ — 생존함수 (`analysis/map_kernel.py`)

**정의**: $h(x)$ = 진입가 대비 수익률 $x$ 인 포지션이 **60분당 닫히는 비율**.

$$\Delta \mathrm{OI}^-(t) \;=\; \sum_j h_j\, C_j(t), \qquad
C_j(t) = \text{수익률이 구간 } j \text{ 에 있는 미결제약정}$$

**5종 / 6년(2020-09~2026-08) / 60분 격자 / 롱숏 분리 / EM 6회 수렴 / 일클러스터 CR1**

| | |
|---|---|
| 노출가중 평균 $h$ | **0.00276** (시간당 0.276%) |
| 손실 쪽 / 이익 쪽 | 0.00248 / 0.00371 = **0.67배** ($t$=−3.5) |
| $x<-30\%$ | **0.01115** — 평균의 **4.04배** ($t$=8.6) |
| $-1.5\sim1.5\%$ (노출 17.7%) | 0.00159 — 가장 낮음 |

**$h$ 는 자발+강제 합산이다.** 그래서 강제 신호가 섞여 들어온다(§5.3).

**추정상 함정 2개 (해결됨)**:
1. 롱숏을 합치면 대칭이 **인위적으로** 만들어진다(첫 판 손익비 1.04, $t$=0.6). 분리 필수.
2. 0 을 구간 경계로 두면 양옆이 거의 완전공선이 돼 ±0.011 짜리 거대 계수가 나온다.
   **0 을 품는 하나의 구간**으로 합칠 것.
3. 비례배분 제거는 **귀무가설 자체**라 찾는 효과를 지운다 → **EM 반복 필수**
   (0.72 → 0.67, 반복해도 1 로 돌아오지 않음 = 편향 탓이 아님).

## 5.2 $f_R$ — 강제청산 위치 (`analysis/fr_kernel.py`)

**HL 격리 포지션 135,668행 / 명목 \$65.8B / 2026-07-31~08-03**

| 구간 | $f_R$ | 누적 |
|---|---|---|
| $<-30\%$ | **23.07%** | 23.07% |
| $-30\sim-20$ | 15.11% | 38.18% |
| $-20\sim-15$ | 10.81% | 48.99% |
| $-10\sim-7$ | 14.78% | 68.12% |

명목가중 레버리지: 중앙 **8.3배**, p75 18.5배, p90 48배.

> **축 정의 (중요)**: $x$ 는 **포지션 자신의 수익률**이다. 청산은 롱이든 숏이든
> **항상 손실 쪽**이므로 $f_R$ 은 **전부 음수 쪽**이다. 첫 판에서 숏을 '가격 방향'
> 으로 놓아 36%가 양수 쪽에 찍혔다 — 오류였고 정정했다.

## 5.3 ★ 교차검증 — 상관 0.906 ★

$h(x)$ 는 **집계 OI**(자발 99%)에서, $f_R$ 은 **HL 청산가**에서. **완전 독립 데이터.**

| 구간 | $h$/평균 | $f_R$ | |
|---|---|---|---|
| $<-30\%$ | **4.04** | **23.07%** | 둘 다 높음 |
| $-10\sim-7$ | **2.28** | **14.78%** | 둘 다 높음 |
| $-15\sim-10$ | 0.08 | 4.34% | 둘 다 낮음 |
| $5\sim7$ | 2.50 | **0%** | **h만** — 순수 익절 |
| $10\sim15$ | 3.38 | **0%** | **h만** — 순수 익절 |

**손실 구간 상관 = 0.906.**

> **레버리지 분포로 예측한 위치에서 실제로 포지션이 사라진다.**
> 두 독립 데이터가 같은 곳을 가리킨다. **지도가 성립할 수 있다는 첫 증거다.**
> 그리고 이익 쪽 $h$ 스파이크는 $f_R$=0 이므로 **순수 행동**임이 확인된다 —
> 예상과 정확히 일치한다.

**한계 3개**: (1) HL 은 **4일치**인데 $h$ 는 6년치다. (2) HL 은 BTC perp OI 의 약 15%
이고 실현 청산가의 70%가 지도에 없다(Q1) — 대표성 미검정. (3) 교차마진(명목 \$895B,
격리의 13.6배)은 $f_R$ 추정 불가.

## 5.4 D-2 — 지도가 $X$ 를 설명하는가 (`analysis/build_map.py`)

**5종 / 6년(2020-09~2026-08) / 214,506구간 / 60분 격자**

$$L(p,t)=\sum_\tau \Delta\mathrm{OI}^+(\tau)\,f_R\!\big(p/p(\tau)\big)\,S(t-\tau)$$

| 조건 | 0~2% 연료 계수 | 증분 $R^2$ |
|---|---|---|
| 전체 구간 | **−2.27** ($t$=−2.5) | +0.032 |
| 하락 상위 20% | +0.77 ($t$=1.4) | +0.004 |
| **하락 상위 5% (캐스케이드)** | **+1.38** ($t$=2.3) | +0.005 |

> **캐스케이드 구간에서만 양수이고, 조건이 강해질수록 커진다.** 설계가 예측한 방향이다.
> 단 **증분 $R^2$ 가 0.005** 로 미미하다.

**여기까지 오는 데 고친 명세 오류 4개** (전부 처음엔 음(−) 계수를 만들었다):

| # | 오류 | 고친 뒤 |
|---|---|---|
| 1 | **이미 탄 연료를 세고 있었다** — 진입 후 최저가 아래를 이미 찍은 코호트도 연료로 계상 | 0~2% 밴드 −0.082 → **+0.292** |
| 2 | **$S$ 와 $f_R$ 이중 차감** — $h$ 가 강제분 포함인데 $f_R$ 이 또 셈 | $h_{\rm vol}$ 분리 |
| 3 | **미결제약정을 롱/숏으로 쪼갬** — 1계약 = 롱1+숏1 이므로 **양쪽 모두 $dq$** | 음수 계수들이 $t$=−10 → −0.4 로 소멸 |
| 4 | **예측 대상을 OI파괴량으로 잡음** + 전방 12시간 창을 임의 설정 | $X$(밀림 거리)로 교체, 조건부로 |

## 5.5 D-3a — ② 깊이 · ③ 유입·취소 (`analysis/book_prob.py`)

**웹소켓 1초 / 21종 / 2026-08-02 15:20 ~ 08-03 23:20 / 223만 초-관측 / 밴드 0.5% / $X$=60초**

| 설정 | $b(\sigma)$ | **$b$(깊이)** | 회복력 | 취소압 | $R^2$ |
|---|---|---|---|---|---|
| $\sigma$ 만 | 0.613 | — | — | — | 0.2099 |
| + $\log D$ | 0.551 | **−0.0746** ($t$=−19.3) | — | — | 0.2275 |
| **+ 회복력·취소압 분리** | 0.583 | **−0.0665** ($t$=−20.8) | **−0.0356** ($t$=−3.8) | −0.0099 ($t$=−1.3) | **0.2284** |

> ### ★ "$V/D$ 기각" 이 명세 오류였음이 확정됐다 ★
> 1초 해상도 + 짧은 지평 + 유입/취소 분리로 바꾸니 **전부 예상 부호**로 나온다.
> 깊이가 두꺼우면 덜 밀리고($b<0$), 유입이 많으면 덜 밀린다.
> **설계의 $\Delta(v,t)$ 가 작동한다.**

**$\Delta$ 정의가 관건이었다**: 1초 **순변화**는 중앙 0.0000 이라 분모를 못 움직인다.
60초 **총유입/총취소**로 바꿔야 한다(각각 재고의 **20.7%** — 1분에 호가창의 1/5 이 갈린다).
그리고 $D+\text{유입}-\text{취소}$ 로 **합치면 오히려 나빠진다**(−0.0738 < −0.0746).
**따로 넣어야 한다.**

**미검출**: 취소압($t$=−1.3, 부호도 예상과 반대) → **유령 유동성 가설 지지 없음**(이 규모에서).
**지지 없음**: 임계형. $\log D$ 구간선형 기울기가 −0.118 / +0.194 / −0.117 / −0.047 로
**부호가 왔다갔다** 한다. 단조 구조가 아니다.

**캘리브레이션**: 위반율 평균편차 0.0552 → **0.0265 (52.1% 감소)**, PIT $D$ 0.098 → 0.050,
핀볼 14.5% 개선. ($n$=81만이라 Kupiec/KS 는 1% 미만 편차도 기각한다 — 실질 지표는 편차다.)

## 5.6 D-3b — ①②③ 전부 결합 (`analysis/d3_join.py`)

**웹소켓 47.9시간 / 21종 / 334만 초-관측 / 코호트 룩백 100일**

| 설정 | $b$(깊이) | **$b$(지도)** | $R^2$ | 증분 |
|---|---|---|---|---|
| + D + 회복력 | −0.090 ($t$=−19.3) | — | 0.2132 | 기준 |
| **+ $L$ 전체** | −0.090 | **−0.014 ($t$=−1.1)** | 0.2132 | **+0.0000** |
| + $L$ 근거리 | −0.091 | −0.014 ($t$=−1.1) | 0.2133 | +0.0002 |
| **비율형 $\log(L/D)$** (설계식) | — | −0.085 ($t$=−7.5) | 0.2008 | **−0.0123** |

캘리브레이션도 M1(0.0133) vs M2(**0.0135**) — 지도를 넣으면 미세하게 **나빠진다.**

> **사전등록 기준 +0.02 에 대해 실측 +0.0000. 불통과.**
> 그리고 **설계식 그대로인 비율형이 $R^2$ 를 0.0123 떨어뜨린다.**

### 단 이 판정의 범위
| | |
|---|---|
| 창 | **47.9시간** |
| **캐스케이드 사건** | **0건** |
| $L/D$ 중앙 | 4.78 |

**평시 구간에서만 잰 것이다.** 지도의 값어치는 정의상 **가격이 연료 구간에 도달할 때**
나타나는데 48시간에 그런 사건이 없었다. D-2 에서도 전체 구간은 음수, 캐스케이드
상위 5%에서만 양수(+1.38)였다 — **같은 패턴**이다.

> **말할 수 있는 것은 "지도가 무용하다" 가 아니라 "평시에는 무용하다" 까지다.**

## 5.7 부수 — 슈팅 전환 시도와 그 실패 (`analysis/momentum.py`)

사건 순간 진입이 −229.6bp 인 이유는 그 뒤로 425bp 가 **더 밀리기** 때문이다.
→ 방향을 뒤집어 **캐스케이드를 타면**? 실측:

| 판 | 사건 | 슈팅 추적150bp | $t$ |
|---|---|---|---|
| 룩어헤드(5분봉 8σ **안에서** 3σ 봉 선택) | 1,005 | +215.5 | 9.6 |
| **실시간 OI (지연 0)** | **3,534** | **−2.0** | **−0.4** |
| 가격만 | 406,568 | −10.8 | −22.3 |

**+215.5 는 생존자 편향이었다** — "3σ로 시작해 결국 8σ까지 간 경우" 만 남았다.
흐지부지된 2,500건이 통째로 빠졌다. **슈팅도 되돌림도 실시간 신호는 0이다.**

---

# 5B. 2026-08-05 세션 — 판정 12개가 뒤집혔다

> 이 절은 **하루 만에 나온 결과**다. 앞 절(§5.1~5.7)과 충돌하는 곳이 있으면
> **여기가 우선**이다. 충돌 지점은 각 항에 명시했다.

## 5.8 A — $\Delta(v,t)$ 프레임 보정 (`analysis/delta_frame.py`)

**웹소켓 2026-08-02~04 / 21종 / 연속 1초쌍 353만**

### 무엇이 틀렸었나
`depth_ws.bands()` 는 **현재 mid 기준** 누적 명목가를 낸다. 그 차분($\Delta$)은
$$B_t(v)-B_{t-1}(v) = \underbrace{[\hat B_t(v)-B_{t-1}(v)]}_{\text{호가창이 얼어 있어도 생김}} + \underbrace{[B_t(v)-\hat B_t(v)]}_{\text{진짜 유입·취소}}$$
mid 가 움직이면 밴드 **경계가 따라 움직여** 호가가 하나도 안 변해도 차분이 0이 아니다.
12개 밴드 지점에서 가격함수 $G_{t-1}$ 을 보간해 새 창틀로 다시 읽으면 분리된다.
합성 검증: 동결 호가창 + mid 이동에서 인공물의 **94.4~99.99% 제거**,
취소+이동이 섞인 경우 정답 대비 **오차 0.5%**.

### 인공물의 크기 — 밴드마다 극단적으로 다르다
| 밴드 | $\Sigma\|$프레임$\|/\Sigma\|$총$\|$ | Var 비 |
|---|---|---|
| b0_05 (5bp) | **0.602** | **0.702** |
| b0_1 | 0.534 | 0.654 |
| b0_5 (D-3a 사용) | 0.241 | 0.138 |
| b2 | 0.146 | 0.040 |
| b10 | 0.008 | 0.000 |

### ★ 좁은 밴드에서 원본은 **거짓 양성**을 만든다
| 밴드 | 취소압 원본 → 보정 | 회복력 원본 → 보정 |
|---|---|---|
| b0_1 | **+0.0246 ($t$=+3.0)** → −0.0070 ($t$=−0.7) | −0.0549(−4.9) → −0.0500(−5.8) |
| b0_2 | −0.0141(−1.0) → −0.0355(−2.5) | −0.0644(−5.1) → −0.0561(−5.0) |
| b0_5 | −0.0288(−2.3) → −0.0172(−1.4) | −0.0593(−6.3) → **−0.0836(−9.3)** |
| b2 | −0.0038(−0.2) → +0.0125(+1.4) | **+0.0059(+0.4)** → −0.0275(−3.0) |

> **b0_1 을 보정 없이 썼다면 "유령 유동성 검출($t$=3.0)" 이라고 발표했을 것이다.**
> 실제로는 인공물이다. **취소압은 보정 후에도 부호가 불안정하고 검출되지 않는다.**
> **회복력은 보정 후 4개 밴드 전부 음수** — 원본은 b2 에서 부호가 뒤집혔다.

### 보정은 정확도만이 아니라 **식별**을 고친다
corr(유입, 취소) b0_5 **0.809 → 0.693**, b0_1 **0.901 → 0.645**. VIF 5.8 → 2.1.
프레임 이동이 유입·취소 **양쪽에 공통 성분**을 심고 있었다.

직교화(활동량 = 평균, 순방향 = 유입−취소):
- **활동량**이 지배한다: −0.057($t$=−7.4) ~ −0.101($t$=−10.1)
- **순방향**(설계의 $\Delta$ 주장): −0.0103 ~ −0.0332, $t$=−0.9~−3.6. 살아있으나 작다.

## 5.9 B — 지정가 버전과 수익의 원천 (`limit_fill.py`, `pnl_source.py`)

**2020-09-03~2026-06-17 / 21종 / 방아쇠 1005건 / 보유 15분 / 왕복 10bp**

기준선 재현: **48.29bp ($t$=2.8)**. `backtest.py` 의 48.3 과 일치.
(샤프는 $t/\sqrt{T}$ 로 낸다 = 1.15. `backtest.py` 의 iid 공식 2.21 은 부풀린 값이다.)

### 고정 오프셋 지정가는 전부 시장가보다 나쁘다
| $\kappa$ (σ 배수) | 체결률 | 시도당bp | 체결당bp |
|---|---|---|---|
| 0 (시장가) | 1.000 | **48.29** | 48.29 |
| 0.5 | 0.902 | 41.85 | 46.37 |
| 1.0 | 0.823 | 36.62 | 44.50 |
| 5.0 | 0.464 | 30.34 | **65.42** |

침투 마진 $\delta$ 0→10bp 로 늘리면 단조 악화 — **결론이 체결 낙관도에 안 걸려 있다.**
체결당만 보면 $\kappa$=5 가 65.42 로 이기는데 체결률이 0.464 다. **생존자 편향의 표본.**

### ★ 역선택을 직접 쟀다
같은 사건을 **시장가로** 잡았을 때의 수익:
| | n | 수익 |
|---|---|---|
| 지정가가 체결된 사건 | 827 | +20.05 bp ($t$=1.0) |
| **체결 안 된 사건** | 178 | **+179.50 bp ($t$=10.9)** |

**도달하지 않는 17.7% 가 전체 수익의 66% 다.** 가격이 내 지정가까지 왔다는 것은
그 사건이 안 튄다는 뜻이다.

### ★ 수익은 되돌림이 아니라 "안 밀린 것" 에서 나온다 (`pnl_source.py`)
| 최저점 시각 | n | 비중 | 평균bp | MAE중앙 | 이익 기여 |
|---|---|---|---|---|---|
| **0분 (진입 순간이 바닥)** | 444 | 44.2% | **+162.6** | −28.6 | **+148.7%** |
| 1~2분 | 169 | 16.8% | +79.7 | −100.8 | +27.8% |
| 3~13분 (밀렸다 돌아섬) | 318 | 31.6% | **−72.5** | −136.6 | **−47.5%** |
| 14~15분 (끝까지 밀림) | 74 | 7.4% | **−190.0** | −146.9 | −29.0% |

**V자는 실재하지만 진입가를 회복하지 못한다**(MFE 중앙 +50.4bp).
즉 이 전략은 **되돌림을 먹는 게 아니라 이미 끝난 사건에 올라타는 것**이다.

### 집중도 — 이것이 현행 설정의 최대 약점이었다
상위 1건 10.2% / 10건(1%) 33.0% / **50건(5%) 76.4%**.
**상위 5% 제거 시 11.6bp, $t$=0.9 — 무너진다.** (상위 1% 제거는 31.9bp, $t$=2.2 로 생존)
MAE p10 **−445bp**. BTCUSDT 142건 평균 5.8bp / 중앙 −6.3 / **승률 42.3%**.

## 5.10 확률적 바닥 추정 — 정적 (`analysis/prob_entry.py`)

$t_0$ 정보만으로 $\log X = \beta'f + \varepsilon$, 워크포워드(200건 후 50건마다 재적합,
1일 퍼지). 특징: $\sigma$, $|z|$, dOI, 직전 3/12바 누적수익, OI 재고, 테이커 롱숏비,
상위트레이더 포지션비, 계좌 롱숏비, 1분 실현변동성, 거래량비.

**OOS 599건 (2023-02-13~2026-06-17). 캘리브레이션 통과** (편차 +0.012/+0.032/−0.009/−0.037/−0.080).

| | 체결률 | 시도당bp | $t$ | 승률 | 손익비 | 샤프 | 최대낙폭 |
|---|---|---|---|---|---|---|---|
| 시장가 | 1.000 | 49.7 | 2.0 | 63.4% | 1.09 | 1.08 | −6588 |
| **α=0.90 / 60분** | 0.180 | **60.1** | 2.8 | 78.7% | 2.86 | **1.51** | −1439 |
| α=0.95 | 0.115 | 45.4 | 2.4 | 79.7% | 2.77 | 1.31 | −1018 |
| α=0.98 | 0.083 | 40.8 | 2.0 | 84.0% | 2.69 | 1.11 | −1226 |

α=0.90 이 **봉우리**(경계 아님). 전/후반 48.6($t$=1.7) / 71.6($t$=2.1) 둘 다 양수.

> **단 우위(+10.4bp)가 2024년 84건에 전부 몰려 있다.** 연도별 기준선 대비:
> 2023 −9.8 / **2024 +157.4** / 2025 −38.2 / 2026 −15.3.
> **위험 개선**(낙폭 4.6배, 승률 +15%p, 손익비 2.6배)은 전 기간에 걸쳐 있다.

## 5.11 두 레그 독립 — 밀림 레그와 손절 (`analysis/two_leg.py`)

### ★ 밀림(슈팅) 레그는 규칙을 붙여도 전부 손실 — 앞선 +93.9bp 는 **철회**
익절/손절/시간정지를 붙인 **16개 조합 전부** −19.6 ~ −35.8bp, $t$=−2.1~−4.6.

> §5.10 계열에서 "체결 시 밀림레그 +93.9bp" 라고 보고했던 것은 **선택 편향**이다.
> 그것은 "되돌림 지정가가 체결된 경우" 만 집계한 값이고, 그 조건은 곧
> "가격이 더 내려간 경우" 다. 독립 규칙으로 재면 **−24.8bp** 다.

이유는 구조적이다: $t_0$ 은 OI 확인을 기다린 시점인데 **그때 이미 31.4% 가 바닥을
지났고 중앙값이 5분**이다. 확인 지연 때문에 밀림의 대부분이 끝나 있다.

### 손절 — 정적 지정가 진입에서는 해롭다
| 되돌림 규칙 (α=0.90) | 시도당bp | $t$ | 샤프 |
|---|---|---|---|
| **손절 없음, 60분** | **60.1** | 2.8 | 1.51 |
| 익절3q 손절8q (익절우선=상한) | 18.7 | 2.4 | 1.32 |
| 익절1q 손절1q (손절우선=하한) | −4.7 | −2.0 | −1.07 |

봉내 동시도달 순서를 모르므로 **손절우선(하한)/익절우선(상한)** 을 둘 다 냈다.
**상한 가정에서도 손절은 수익을 죽인다.** 손절 폭을 넓힐수록 단조 개선 = "붙이지 마라"의
연속판. 꼬리: 손절 없음의 **최악 1건 −1432bp**, p01 −349.

> **단 이것은 정적 지정가 진입 기준이다. 시장가 진입에서는 반대다 (§5.15).**

## 5.12 ★★ 정적 지정가는 **원리상** 바닥에 못 걸린다 (`analysis/residual_life.py`)

분위 지정가의 **체결 조건이 곧 $X \ge q_\alpha$** 다. 즉 체결되는 순간은 언제나
"바닥이 내 주문보다 아래" 인 경우다. 어떤 $\alpha$ 를 골라도 이 구조는 안 바뀐다.

평균잔여수명 $m(u)=E[X-u \mid X\ge u]$, $q_{50}$ 배수 단위 (OOS 599건):
| $u$ | 체결률 | $m(u)$ | 중앙 잔여 |
|---|---|---|---|
| 0.25 | 0.810 | 4.76 | 1.25 |
| 1.00 | 0.509 | 6.65 | 1.83 |
| 2.00 | 0.324 | 9.19 | 2.99 |
| 5.00 | 0.162 | **14.19** | 3.53 |

**단조 증가한다.** 지수분포면 상수여야 한다 — **파레토 꼬리이고 바닥이 계속 도망간다.**
실측 확인: 체결 직후 MAE 중앙이 α=0.50 −174.0 / 0.70 −211.5 / **0.90 −292.7 bp**.
**깊이 걸수록 악화된다.**

> 그러므로 §5.11 의 "손절이 해롭다" 는 손절이 나빠서가 아니라
> **바닥을 못 맞혀서 손절할 수밖에 없는 자리에 들어가기 때문**이다.

## 5.13 동적 지정가 — 바닥 포착 성공, 수익 실패 (`analysis/dyn_entry.py`)

매 분 **현재 저점 $M_u$ 기준으로 남은 밀림 $R_u$ 를 다시 추정**해 주문을 옮긴다.
$R_u=0$ 이면 지금이 바닥. **OOS 캘리브레이션 통과**(편차 −0.016~+0.032),
**$R=0$ 인 분이 67.5%**.

| | 체결률 | 시도당bp | $t$ | 샤프 | **체결후 MAE** |
|---|---|---|---|---|---|
| 정적 α=0.90 | 0.160 | **41.4** | **2.4** | **1.32** | −201.9 |
| **동적 α=0.30** | 0.608 | 13.9 | 0.8 | 0.42 | **−71.7** |
| 동적 + 낙폭≥8σ 필터 | 0.195 | 18.1 | 1.8 | 0.96 | −180.1 |

**MAE 를 2.8배 개선한다(바닥 포착 성공). 수익은 3배 나빠진다.**

> **바닥을 맞히는 것과 돈을 버는 것은 다른 문제다.**
> 수익은 **가격 이탈의 크기**에서 나오지 타이밍 정밀도에서 나오지 않는다.
> 동적 주문은 첫 소규모 저점(대기 중앙 2분)에서 체결되는데, 거기서 정확히 바닥을
> 잡아봐야 이후 상승이 중앙 15.9bp 라 수수료 7bp 를 빼면 남는 게 없다.
> **정적 α=0.90 이 이겼던 이유가 확정됐다 — "바닥을 잘 맞혀서" 가 아니라
> "크게 이탈했을 때만 체결돼서" 다.**

## 5.14 시간 지평과 회전율 (`horizon.py`, `residual_life.py` §3)

### 60분은 최적이 아니라 내가 만든 격자의 경계였다
$W$(지정가 유효) × emax(보유) 격자에서 **두 방향 모두 단조 증가**. 봉우리가 없다.
그리고 창을 120분으로 잘라 저장해 60분 너머를 볼 수 없다.

**시간도 확률모형으로 추정 가능하다** — $\log T$ 를 같은 특징·같은 워크포워드로.
OOS 편차 −0.030/+0.051/**+0.006**/−0.044. 실제 $T$: p25 **0분** / p50 **5분** / p90 **46분**.
사건마다 100배 차이난다 — 하나로 고정할 값이 아니다.

| | 시도당bp | $t$ | 샤프 | 최악 1건 | 보유 중앙 |
|---|---|---|---|---|---|
| 고정 60/60 | **60.1** | 2.8 | 1.51 | −1432 | 60분 |
| 상태의존 β=0.90, emax=τ | 48.2 | **3.1** | **1.68** | −680 | 30분 |
| 상태의존 β=0.70, emax=2τ | 44.5 | 2.9 | 1.59 | **−287** | 25분 |

**상태의존이 꼬리를 5배 줄인다.**

### 회전율 — HFT 영역에는 도달할 수 없다
보유 시간별 (α=0.90, $W$=60):
| 보유 | 시도당bp | 샤프 | 분당bp | 최악 1건 |
|---|---|---|---|---|
| 1분 | 2.7 | 0.18 | 15.16 | −810 |
| **5분** | 35.8 | **1.57** | **39.66** | **−493** |
| 15분 | 49.7 | 1.46 | 18.38 | −817 |
| 60분 | 60.1 | 1.51 | 5.56 | −1432 |

**평균 동시보유 0.0001~0.0037.** 신호가 연 179건뿐이라 자본이 99.97% 논다.
**자본은 애초에 제약이 아니다.** 짧은 보유의 이득은 자본효율이 아니라 **꼬리 축소**다.

## 5.15 ★★ 방아쇠 재설계 — 여기서 가장 큰 개선이 나왔다

### (1) 우위는 K 에 **단조 증가**한다 (`analysis/trigger_sweep.py`)
dOI=−0.01 기준, **총이익bp = 비용 차감 전 순수 우위**:
| K | 연간건수 | 총이익bp | $t$ | 연간총bp |
|---|---|---|---|---|
| 1.5 | 2,951 | **1.9** | 0.8 | −24,010 |
| 3.0 | 1,646 | 6.2 | 1.9 | −6,301 |
| 6.0 | 585 | 25.0 | 3.1 | +8,745 |
| 10.0 | 220 | **74.4** | 3.2 | +14,199 |
| 25.0 | 29 | **474.6** | 2.5 | +13,437 |

**건수를 13배 늘리면 우위가 39배 사라진다.** 3개 dOI 수준 전부에서 같은 기울기다.
비용 10bp 가 K≤4 에서 우위를 통째로 먹는다. **동시보유는 어디서도 1 을 못 넘고,
넘는 영역(K=1.5, 0.26)은 우위가 이미 0 이다.**

> **HFT 논리(작은 우위 × 큰 횟수)는 이 신호에 적용되지 않는다.**
> 우위가 희소성에서 나오므로 회전율과 정면으로 상충한다. **레버는 선별이다.**

### (2) 방향 비대칭 (`analysis/asymmetry.py`, K=12/dOI=−0.005, 1118건)
| | n | 시도당bp | $t$ | 낙폭 |
|---|---|---|---|---|
| 매수 (하락 후 반등) | 873 | **88.1** | 3.2 | −2,799 |
| **매도 (상승 후 되돌림)** | 245 | −11.0 | **−0.3** | −6,265 |

> **정확한 진술: 매도는 우위가 "검출되지 않는다"($t$=−0.3). "마이너스다" 가 아니다.**
> 하위 칸(매도×하락장 95건 −79.8bp)은 $t$=−0.9 로 **유의하지 않다** — 그 점추정에
> 서사를 붙이면 §3.7 의 통계 오류를 반복하는 것이다. **매수만 쓰는 근거는
> "매도에 우위가 없다" 까지다.**

### (3) ★ 1분봉 방아쇠 + 중복제거 폐지 (`analysis/fast_trigger.py`)
5분봉을 쓴 이유는 OI 스냅샷이 5분 간격이라서다. **가격만 1분봉으로 옮기고
dOI 는 직전에 이미 알려진 5분 값을 쓰면** 확인 지연이 최대 10분 → **1분**이 된다.

| K=8 | 연간건수 | 시도당bp | $t$ | 연간총bp | 샤프 |
|---|---|---|---|---|---|
| **gap=1분 (중복제거 없음)** | **579** | 23.7 | **5.2** | **13,758** | **2.16** |
| gap=5분 | 463 | 16.3 | 3.4 | 7,559 | 1.40 |
| gap=15분 | 434 | 13.1 | 3.1 | 5,694 | 1.29 |

**중복제거가 2.4배 손해였다.** 전/후반 29.0($t$=4.0) / 18.4($t$=3.4) 둘 다 통과.

### (4) 동시보유 중첩은 실제로는 없다
| 설정 | 최대동시 | **심볼최대** | p99 | 자본정규화 | 낙폭정규화 |
|---|---|---|---|---|---|
| K=8 gap=1분 | 19 | **2** | 14 | **724** | −150 |
| K=8 gap=5분 | 17 | 2 | 13 | 445 | −146 |
| **K=10 gap=1분** | 18 | **2** | 14 | 718 | **−90** |

**같은 심볼에 동시에 열리는 포지션이 최대 2개다.** 최대동시 19 는 **심볼 간 동시성**
(시장 전체가 같이 무너지는 순간)에서 오는 것이지 심볼 내 중첩이 아니다.
gap 을 5분으로 늘려도 19→17 로 거의 안 줄고 건수만 40% 잃는다.
**따라서 '진입 후 N분 재진입 금지' 규칙은 불필요하다.**

### ★ 현재 최선 설정
| | |
|---|---|
| 방아쇠 | **1분봉 $z_1 \le -10$** 그리고 **직전 알려진 5분 $dOI \le -0.5\%$** |
| 방향 | **매수만** |
| 중복제거 | **없음** |
| 진입 | 다음 1분봉 시가, **시장가** |
| 청산 | 익절 $8\sigma_{5m}$ / 손절 $2\sigma_{5m}$(+슬리피지 5bp) / **15분** 중 먼저 |
| 연간 건수 | **361** |
| 시도당 | **35.8 bp ($t$=5.8)** |
| 승률 / 손익비 | 42.7% / **2.73** |
| 샤프 | **2.38** |
| 필요 자본 | 19단위 (심볼당 2) |

**세션 시작 시점(48.3bp / 샤프 1.16 / 연 174건 / 연간총 8,389)과 비교:
연간총 8,389 → 12,923, 샤프 1.16 → 2.38.**

## 5.16 오더북을 넣으면 — 30초 해상도에서는 증분 0 (`book_feat.py`, `book_dyn.py`)

`book_depth` 일별 파일이 **27,140개 / 21종 / 2023-01-01~2026-08-03 / 30초 / 7.8GB**
로 다 있다. (내가 구 단일 파일 `book_depth/<SYM>.parquet` 만 읽어 커버리지가
26.6% 인 줄 알았다. 실제는 사실상 100%.)

특징: `ldep`(±1% 깊이/과거1일평균), `limb`(불균형), `lslope`(±5%/±1%),
`ddep5`·`ddep30`(깊이 변화 = 회복력·취소압). 654건(65%)에 부착.
신호 자체는 실재한다 — **이벤트 시점 깊이가 평소의 0.41배**(ldep 중앙 −0.897),
**직전 5분에 57% 감소**(ddep5 −0.837).

| | 중앙절대로그오차 (OOS) |
|---|---|
| 정적 $X$ — 가격·OI만 | **1.0610** |
| 정적 $X$ — +오더북 | 1.1100 |
| 동적 $R_u$ — 가격만 | **1.0851** |
| 동적 $R_u$ — +오더북 | 1.1385 |

하나씩 넣어도 마찬가지 (모수 증가 문제가 아니다):
ldep **−0.0186** / limb +0.0015 / lslope −0.0008 / **ddep5 −0.0118** / ddep30 +0.0036.
**설계의 핵심인 깊이와 회복력이 가장 크게 악화시킨다.**

### A(§5.8)와 모순이 아닌 이유
| | A (`delta_frame`) | 여기 (`book_dyn`) |
|---|---|---|
| 해상도 | **1초**, ±0.05~2% | **30초**, ±1~5% |
| 목표 | 다음 15/60/300**초** 밀림 | 최대 60**분** 남은 밀림 |
| 표본 | 평시 353만 초-관측 (캐스케이드 0건) | 캐스케이드 **643건** |
| 경쟁 특징 | $\sigma$ 하나 | 17개 (z·dOI·OI재고·포지션비·거래량·진행낙폭…) |

마지막 줄이 결정적일 수 있다. **캐스케이드 안에서는 깊이가 이미 가격·OI 정보와
중복된다** — "깊이가 평소의 0.41배" 는 "z 가 크다" 와 사실상 같은 말이다.

> **설계의 ②③은 과거 데이터로 검증이 안 된다.** 30초 해상도에서는 신호가 안 잡히고,
> 1초 데이터에는 캐스케이드가 0건이다. **웹소켓에 캐스케이드가 쌓일 때까지 열린 문제.**

---

## 5.17 ★★ D-10 견고성 — §6.17 의 성적은 **손절 모형 인공물**이었다 (`analysis/robust.py`)

**데이터 기간**: Binance USD-M 21종. 1분봉 패널 2020-09-01 ~ 2026-07-30,
사건 2020-09-02 ~ 2026-07-29. 거래 발생일 444일. 왕복 10bp.
**실행일**: 2026-08-05. 귀속 검증은 `scratchpad/attrib.py`.

목적은 §6.17 의 설정이 **격자에서 골라낸 한 칸** 이상인지 보는 것이었다.
전/후반 분할만 통과한 상태였고 심볼별·비용별·체결가정별로는 통제된 적이 없었다.

### 5.17.1 결정적 발견 — 진입 봉을 건너뛴 것이 성적 전부였다

§3.11 에 원인과 귀속을 적었다. 요지만:

- 진입 1분 안에 손절선을 뚫는 거래가 **47.5%(817/1,721)**
- 원본은 그중 **164건을 평균 +224bp 익절**로 처리
- 두 모형 총합 차이의 **100.9%** 가 이 817건에서 나옴
- **30.4bp($t$=4.5) → −2.6bp($t$=−0.5)**

`asymmetry.py:99` 에도 같은 줄이 있다. **§6.17 판정 #3("손절이 시장가 진입엔
유익, 샤프 1.67")의 근거였던 조합3(연 154건 40.8bp 샤프 1.82)도 오염됐다.**

### 5.17.2 ★ 어떤 손절도 이기지 못한다 (정정 모형, K=10 gap=5 익8σ₅ 상한15분)

| 손절 | 시도당bp | $t$ | 손절발동률 |
|---|---|---|---|
| 터치 1.0σ₅ | −5.6 | −1.9 | 0.81 |
| 터치 2.0σ₅ | −2.6 | −0.5 | 0.69 |
| 터치 3.0σ₅ | −3.2 | −0.5 | 0.60 |
| 터치 4.0σ₅ | −4.1 | −0.6 | 0.53 |
| 터치 8.0σ₅ | −3.4 | −0.4 | 0.34 |
| 터치 12σ₅ | −4.1 | −0.3 | 0.23 |
| **종가** 1.0~6.0σ₅ | −8.0 ~ −9.4 | −0.8 ~ −0.6 | 0.28~0.54 |
| **없음** | **+29.3** | **+2.7** | 0.00 |

익절×손절 격자 20칸에서 **$t>2$ 인 칸 0개.** 봉내 순서를 익절 우선(낙관 상한)으로
놔도 +5.2bp($t$=1.0). 웍 회피용 종가 손절은 오히려 더 나쁘다.

> **손절은 이 전략에서 구조적으로 해롭다.** 되돌림을 먹는 거래는 **먼저 역행하도록
> 설계된** 거래다. −2σ₅ 손절은 반등 직전의 역행을 골라서 실현시킨다.
> §5.12 의 평균잔여수명 단조 증가가 같은 사실을 반대편에서 말한 것이다.

### 5.17.3 정정 후 최선 설정

손절 제거 · 익절 8σ₅ · 상한 30분 · 매수만 · K=10 · $dOI\le-0.5\%$ · 왕복 10bp:

| | gap=5 (사용자 지시) | gap=1 | ~~§6.17 기록~~ |
|---|---|---|---|
| 연간 건수 | 292 | 361 | 361 |
| 시도당 | 34.9bp ($t$=3.1) | 48.9bp ($t$=4.2) | ~~35.8bp ($t$=5.8)~~ |
| 승률 / 손익비 | 64.2% / 0.88 | 67.6% / 0.88 | ~~42.7% / 2.73~~ |
| 연간총bp | 10,187 | **17,662** | ~~12,923~~ |
| 샤프 | 1.26 | **1.74** | ~~2.38~~ |
| 최대낙폭 | −5,217bp | −7,612bp | ~~−1,626bp~~ |
| 최대동시 (심볼최대) | 27 (3) | 39 (6) | ~~18 (2)~~ |

**기준선(연 174건 48.3bp 연간총 8,389 샤프 1.16)은 여전히 이긴다** — 기준선은
시간청산만 쓰므로 이 결함의 영향을 받지 않는다. 다만 배수가 훨씬 작다.

### 5.17.4 견고성 (정정 모형 기준)

| 축 | 결과 |
|---|---|
| **심볼** | 19/21 양수. $t>2$ 는 **6/21**(gap=5)·**10/21**(gap=1). 최악 leave-one-out $t$=2.8·3.8. TRXUSDT 만 유의하게 음수 |
| **군** | 알트 16종 38.4bp($t$=2.9) vs 메이저 5종 26.6bp($t$=2.3). **우위가 알트에 있다** |
| **비용** | 손익분기 **왕복 44.9bp** (실측 9bp). $t\ge2$ 유지 한계 22.1bp, $t\ge3$ 은 10.7bp |
| **진입 슬리피지** | 1bp당 약 1bp. +10bp 까지 $t$=3.4 유지 |
| **익절 큐 버퍼** | +20bp 초과 요구해도 26.8bp($t$=3.9) — 둔감 |
| **이익 집중** | 상위 5% = 순이익의 **65.7%**(gap=5)·47.4%(gap=1) |
| **일블록 부트스트랩** | 전체 $P(\le0)$=0.0015·0.0000 / **상위 5% 제거 후 0.151**·0.017 |
| **날짜 집중** | 상위 10일 = 이익의 **64.2%**. 제거 시 $t$=1.4 |
| **왼쪽 꼬리** | 최악 1건 **−2,525bp**, p1=−792bp, p5=−308bp. 손절이 없는 대가 |
| **연도** | 2021 −125bp(n=47) 음수. 2022·2024 강함, 2023·2025·2026 보통. **2024가 지배** |
| **σ 역가중 사이징** | 집중은 절반(24.0%→11.3%)으로 줄지만 gap=5 에선 $t$=1.4 로 유의성 상실 |
| **종목 선택 편향** | 2021 이후 상장 2종(SUI·WLD)이 109건에 **109.4bp $t$=5.7**. 빼도 29.9bp($t$=2.6) 로 살지만 극단값이다 |

**가장 약한 지점**: gap=5 + 상위 5% 제거 시 부트스트랩 $P(\le0)$=**0.151**.
gap=1 은 같은 검정에서 0.017 로 버틴다. **회전율이 높을수록 꼬리 의존이 줄어든다** —
5분 재진입 금지는 통계적 견고성 면에서도 비용이다.

### 5.17.5 ★ 청산(OI) 부품은 장식이 아니다 — 정정 후 오히려 필수가 됐다

| $dOI$ 조건 | n | 시도당bp | $t$ | 연간총bp |
|---|---|---|---|---|
| **없음 (가격만)** | 7,535 | **6.5** | **0.7** | 8,242 |
| $\le-0.001$ | 3,293 | 18.9 | 1.8 | 10,541 |
| $\le-0.002$ | 2,672 | 22.8 | 1.9 | 10,338 |
| $\le-0.005$ | 1,721 | 34.9 | **3.1** | 10,187 |
| $\le-0.010$ | 949 | 43.0 | 2.5 | 6,924 |
| $\le-0.020$ | 335 | 46.4 | 1.4 | 2,640 |

> **가격만으로는 유의하지 않다($t$=0.7).** 오염된 모형에서는 가격만으로도 $t$=4.5 라
> "$dOI$ 는 장식" 처럼 보였다. 정정하면 반대다. **설계의 청산 부품이 정보를 담는다.**

### 5.17.6 ★★ 미해결 — 보유 지평에 내부 최적이 없다

240분까지 밀어도 꺾이지 않는다 (gap=5, 손절 없음, 익절 8σ₅):

| 상한 | 시도당bp | $t$ | 샤프 | 최대동시 | 자본정규화 |
|---|---|---|---|---|---|
| 15분 | 29.3 | 2.7 | 1.12 | 27 | 316 |
| 30분 | 34.9 | 3.1 | 1.26 | 27 | 377 |
| 60분 | 42.9 | 4.5 | 1.87 | 27 | 463 |
| 120분 | 54.3 | 5.5 | 2.28 | 27 | 586 |
| **240분** | **65.0** | **6.0** | **2.45** | 27 | **702** |

익절도 빼면 240분에 157.1bp($t$=3.2, 연간총 45,784). **모든 지표가 단조 증가한다.**
15분·30분·60분은 전부 내가 임의로 정한 격자 경계였다 (§3.7 의 오류를 또 반복했다).

**이것이 전략의 성격을 바꾼다.** 4시간 보유는 미시구조 거래가 아니다.

### 5.17.7 그래도 방아쇠는 실재한다 — 귀무가설 대조 통과

각 이벤트와 **같은 심볼·같은 날**에서 무작위 분 20개를 뽑아 동일 규칙으로 청산
(대조 34,420건). 심볼 구성과 달력을 동시에 통제한다.

| 상한 | 이벤트bp | 대조군bp | 차이bp | 차이 $t$ |
|---|---|---|---|---|
| 15분 | 75.3 | −14.5 | **+89.7** | 2.8 |
| 30분 | 92.2 | −17.3 | +109.5 | 2.2 |
| 60분 | 106.7 | −24.6 | +131.3 | 2.9 |
| 120분 | 128.1 | −37.5 | +165.6 | 3.4 |
| 240분 | 157.1 | −55.5 | **+212.5** | **4.1** |

> 같은 날 같은 종목을 무작위 시각에 사면 **잃는다**(−14.5 → −55.5bp).
> 우리 방아쇠 시각에 사면 딴다. 차이는 지평이 길수록 **커진다.**
> **장기 단조 증가는 표본 기간의 상승 드리프트가 아니다.**

---

# 6. 이미 확정된 사실 (다시 검정하지 말 것)

| 사실 | 수치 | 출처 |
|---|---|---|
| 되돌림은 실재하고 청산 특유다 | 위약 대비 차이 $t$=2.4 / 2.1 | `scale_check.py` |
| 밀림 거리 $\propto \sigma$ | $b_1$ = **1.017** ($t$=49.7) | `realtime_trigger.py` |
| 조건부 분포는 교정 가능 | Kupiec 0.251/0.499/0.751 | `realtime_trigger.py` |
| 정보를 가진 방아쇠는 OI 급감 하나 | 프린트 $t$=−0.1 vs OI +48.3bp | `liq_trigger.py`/`backtest.py` |
| 진입 타이밍이 결과를 뒤집는다 | 사건 순간 시장가 −229.6bp | `intra_event.py` |
| 사건 후 방향 정보는 소멸 | 청산방향/반대 = 0.96, 47.3% | `sigma_model.py` |
| 자기여기 증폭은 없다 | $A$=1.44, 되돌림과 **음**의 관계 | `amplification.py` |
| 5초 OI는 물리적으로 가능 | 갱신 4.3~6.5초, 잡음 0.06bp vs 임계 200bp | `oi_fast.py` |

### 2026-08-05 에 추가된 것

| 사실 | 수치 | 출처 |
|---|---|---|
| **정적 지정가는 원리상 바닥에 못 걸린다** | $m(u)$ 4.76 → 14.19 **단조 증가** | `residual_life.py` §5.12 |
| 수익은 타이밍이 아니라 **이탈 크기**에서 나온다 | 동적이 MAE 2.8배 개선하고도 수익 3배 악화 | `dyn_entry.py` §5.13 |
| 돈은 **"안 밀린" 사건**에서 나온다 | $t_{MAE}$=0분 44.2%가 이익의 148.7% | `pnl_source.py` §5.9 |
| **우위는 방아쇠 강도에 단조 증가** | K 1.5→10 에서 총이익 1.9→74.4bp | `trigger_sweep.py` §5.15 |
| **HFT 영역(동시보유≥1)에 도달 불가** | 도달 지점의 우위가 0 | `trigger_sweep.py` §5.15 |
| 매도 방향에 우위가 **검출되지 않는다** | −11.0bp, $t$=−0.3 (n=245) | `asymmetry.py` §5.15 |
| **중복제거는 손해다** | gap 1분 vs 15분 = 2.4배 | `fast_trigger.py` §5.15 |
| 심볼 내 포지션 중첩은 없다 | **심볼최대 동시보유 2** | `fast_trigger.py` §5.15 |
| 밀림(슈팅) 레그는 못 먹는다 | 16개 조합 전부 −19.6~−35.8bp | `two_leg.py` §5.11 |
| 30초 오더북은 증분 정보가 없다 | 5개 특징 전부 OOS 오차 악화 | `book_dyn.py` §5.16 |

### 2026-08-05 D-10 이 추가한 것

| 사실 | 수치 | 출처 |
|---|---|---|
| **손절은 어떤 폭·어떤 판정에서도 해롭다** | 격자 20칸 중 $t>2$ **0칸**. 없음만 +29.3bp($t$=2.7) | `robust.py` §5.17.2 |
| **시장가 진입에서 진입 봉을 빼면 안 된다** | 47.5%가 진입 1분 안에 손절선 돌파 | §3.11 |
| **청산(OI) 부품은 필수다** | 가격만 $t$=0.7 → $dOI\le-0.5\%$ $t$=3.1 | `robust.py` §5.17.5 |
| **방아쇠는 같은 날 무작위 시각을 이긴다** | +89.7 ~ +212.5bp, $t$=2.2~4.1 | `robust.py` §5.17.7 |
| 비용 여유가 넓다 | 손익분기 왕복 **44.9bp** (실측 9bp) | `robust.py` §5.17.4 |
| 우위가 **알트에 있다** | 알트 38.4bp vs 메이저 26.6bp | `robust.py` §5.17.4 |
| 보유 지평에 **내부 최적이 없다** | 240분까지 단조 증가, 샤프 1.12→2.45 | `robust.py` §5.17.6 |

### 철회된 것 (다시 인용하지 말 것)

| 철회 대상 | 실제 | 출처 |
|---|---|---|
| **"1분봉 방아쇠로 샤프 2.38"** | **손절 모형 인공물.** 정정 후 **1.74**(gap=1)·1.26(gap=5) | **§3.11, §5.17.1** |
| **"손절이 시장가 진입엔 유익(샤프 1.67)"** | **정반대.** 모든 폭에서 음수 | **§5.17.2** |
| **"승률 42.7% 손익비 2.73"** | 손절이 만든 분포. 정정 후 **64.2% / 0.88** | **§5.17.3** |
| **"최대낙폭 −1,626bp"** | 손절이 있다는 가정. 정정 후 **−5,217bp** | **§5.17.3** |
| **"$dOI$ 필터는 장식"**(D-10 1차 판독) | 오염 모형에서만 그렇다. 정정 후 **필수** | **§5.17.5** |
| "밀림 레그 체결 시 +93.9bp" | **선택 편향.** 독립 규칙으로 −24.8bp | §5.11 |
| "매도×하락장이 낙폭의 주범" | n=95, **$t$=−0.9. 유의하지 않다** | §5.15(2) |
| "b0_1 에서 유령 유동성 $t$=+3.0" | **프레임 인공물.** 보정 후 $t$=−0.7 | §5.8 |
| "60/60 이 격자 최적" | **격자의 경계**였다. 봉우리가 아니다 | §5.14 |
| "15분·30분·60분 보유" | 전부 **격자 경계.** 240분까지 꺾이지 않는다 | **§5.17.6** |
| 샤프를 `mean/sd*√(연이벤트수)` 로 | 횡단면 의존 무시. **$t/\sqrt{T}$** 를 쓴다 (2.21 vs 1.15) | §5.9 |
