# 목표 설계 — 이 연구가 최종적으로 만들려는 것

> ## 🔴 2026-08-04 — 정본이 `DESIGN_LOCK.md` 로 이동했다
> 같은 오독이 반복돼서(A-2 를 "지도 실패" 로, $V$ 를 "청산만" 으로) 설계·오독방지·
> 구현순서를 한 문서에 고정했다. **충돌하면 `DESIGN_LOCK.md` 가 맞다.**
> 특히 두 가지가 이 문서 시점 이후로 **확정**됐다:
> 1. $V$ = **총 디레버리징** (격리+교차+자발). 청산 프린트는 OI 감소의 1%뿐이고
>    그것만 쓴 방아쇠는 정보량 0($t$=−0.1)이었다.
> 2. 설계는 **지도 + 오더북 + 유입·취소의 합**이다. 지도 단독 검정(A-2)의 실패는
>    설계의 실패가 아니다.

**이 문서는 설계 서술의 원본이다.** 다른 문서(MODEL.md, MECHANISM.md, EXPLAINER.md)와
충돌하면 이 문서가 맞다. 현재 돌아가는 페이퍼(고정 2%)는 이 설계가 **아니라**
넘어야 할 기준선이다.

> **지금 어디쯤인지는 `STATUS.md`.** 무엇이 검정됐고 무엇이 대용품이었는지 한 장으로.
> 요약: **설계의 방정식은 검증됐고, 설계의 입력(청산맵)은 아직 없다.**
> 실패한 검정들은 전부 그 입력의 **대용품**이었고, 그 실패가 보여준 것은
> "대용품이 없다 — 지도가 선택이 아니라 필수"다.

작성 2026-08-02. 근거 수치의 데이터 기간은 각 항목에 표기.

---

## 1. 설계 (한 문단)

> **현재가 기준 상승·하락 양방향으로 격리 OI가 여러 가격대에 걸쳐 쌓여 있다.
> 히트맵으로 그 OI 깊이를 미리 파악해 두고, 가격이 한쪽으로 움직이면 그에 따라
> 오더북도 변한다. 그 변화를 **매 순간 다시 읽어**, 그 격리 OI가 강제청산될 때
> 가격이 얼마나 밀릴지를 **계속 재계산**한다. 그렇게 **캐스케이드가 바닥을 칠
> 가격대를 확률적으로 근사**하고 그 자리에 지정매수를 걸어둔다.**

완벽히 맞출 필요는 없다. **확률적 근사면 된다.** 아래 §2.3이 근사만 되어도 얼마나
크게 이기는지를 보여준다.

### 1.1 왜 확률이어야 하는가 — 호가는 확정 물량이 아니다

MM과 다른 참여자들이 호가를 **넣었다 뺐다** 한다. 이건 확정할 수 없다.
따라서 "호가가 $D_t$ 이니 $V$ 를 먹으면 여기까지 간다"는 **결정론적 계산이 성립하지
않는다.** 실제로 먹을 수 있는 깊이는 $D_t \cdot W$ 이고 $W$(잔존율)는 사실상 미관측이다.
실측 $1/W$: **중앙 3.4배 / p90 17배**, 극단일엔 109~523배
(`w_survival.py`, 21종 636이벤트). $W_{pre}$ 중앙 0.997 이므로
**트리거 시점 자체는 얇지 않다 — 붕괴는 트리거 이후에 일어난다.**

**$W$ 를 확률변수로 다루는 것, 그것이 이 설계가 확률모델인 이유다.**

### 1.2 그리고 $W$ 는 잡음이 아니다 — $L$ 과 얽혀 있다

> OI가 많이 쌓여 있는 쪽을 터뜨리려는 유인이 시장에 있다.

이 가설이 맞으면 **$W$ 와 $L$ 은 독립이 아니다.** $L$ 이 두꺼운 가격대에서 호가가
더 많이 빠진다. 그러면 두 항이 **같은 방향으로** 민다:

$$\log X \;=\; a + \gamma\Big(\log \underbrace{V(u)}_{\textstyle\int L(p)dp}
\;-\; \log D_t \;-\; \log \underbrace{W(u)}_{L(u)\text{의 감소함수}}\Big)$$

$L$ 이 두꺼운 곳에서는 **물량이 더 쏟아지고 동시에 받아줄 호가가 더 사라진다. 곱해진다.**
이것이 $V/D_t$ 가 0.65% 에 불과한데도 가격이 9.2배 더 밀리는 이유의 후보다.

**문헌**: Brunnermeier & Pedersen (2005) *Predatory Trading*, Osler (2005) FX 스탑 캐스케이드.

**비판적 단서**: MM은 스프레드로 벌지 방향으로 벌지 않으므로 "터뜨린다"는 **의도**는
MM보다 방향성 트레이더에 더 맞는다. MM 쪽은 **역선택 회피**로 호가를 뺀다.
그런데 **관측 결과는 같다** — OI 두꺼운 쪽 호가가 사라진다.
어느 이야기가 맞든 예측이 같으므로 **검정에는 오히려 유리하다.**

### 1.3 이것이 $L(p)$ 의 가치를 되살린다

| | $L(p)$ 의 역할 |
|---|---|
| 초기 모델 | $V$ 예보만. 그런데 실현 청산은 호가의 0.65% 뿐이라 약하다 |
| **§1.2 구조** | $V$ **와** $W$ 를 **동시에** 예보. 곱해지므로 훨씬 강하다 |

`PROB_MODEL.md` §9 · §6.5.

---

## 2. 왜 이 형태인가 — 각 요소의 근거

### 2.1 왜 "가격대별" 인가

격리마진 포지션은 청산가가 **가격에 고정**되어 있다. 가격이 그 가격대에 닿으면
그 물량은 강제로 시장가 매도된다. 따라서 어느 가격대에 얼마가 쌓여 있는지를 알면
"거기까지 가면 얼마가 더 쏟아지는가"를 알 수 있다.

지금 쓰는 `dOI`(5분간 전체 OI 변화율)는 **스칼라 하나**다. 어느 가격대의 OI가
빠졌는지 모른다. 그 정보로 바닥 위치를 맞히려는 시도는 원리적으로 불가능하며,
실제로 세 가지 방법으로 시도해 전부 실패했다(§5).

### 2.2 왜 "오더북과 대조" 인가

밀리는 정도는 청산 물량 자체가 아니라 **청산 물량 대비 받아줄 대기물량**이 정한다.
같은 $10M 청산이라도 호가가 $100M이면 안 밀리고 $1M이면 크게 밀린다.

실측: 캐스케이드 중 호가 깊이가 **순간 109~561배 증발**한다
(2025-10-10: BTC 114× / ETH 206× / SOL 561×, `DEPTH_FINDINGS.md`).
즉 분모가 상수가 아니며, 실시간으로 봐야 한다.

### 2.3 왜 "바닥"에 걸어야 하는가 — 정량화된 근거

**완벽 예지로 바닥에 진입했을 때 vs 현행 트리거 −2%** (21종 990이벤트, 2020-09~2026-06):

| 진입 위치 | 1분 보유 | 3분 | 15분 |
|---|---|---|---|
| 실제 바닥 *(도달 불가·아래 경고)* | +156.1bp | +202.5bp | +257.5bp |
| 바닥 +0.25% | +131 | +177 | +232 |
| 바닥 +0.5% | +106 | +152 | +207 |
| 바닥 +1.0% | +56 | +102 | +156 |
| 바닥 +2.0% | **−44** | +2 | +56 |
| **현행: 트리거 −2%** | **−9.9bp** | +38.8 | +87.6 |

> **바닥에서 1%만 벗어나도 수익의 3분의 2가 날아간다.**
> 진입 정밀도가 이 전략의 전부다.
>
> **이 민감도는 유효하다** — 아래 경고와 무관한 별개의 측정이다.

> ## ⚠ +156bp 는 대부분 청산과 무관하다 (2026-08-02 플라시보)
>
> **21종, 1,005 vs 8,400 vs 2,109, 60분 창, 7bp 차감**
>
> | 표본 | n | 1분bp | 15분 | 승률(1분) |
> |---|---|---|---|---|
> | 청산 이벤트 | 1,005 | 155.8 | 256.8 | **99.0%** |
> | **무작위 시점** | 8,400 | **17.5** | 51.2 | **81.0%** |
> | **같은 하락, OI 유지** | 2,109 | **130.2** | 248.7 | — |
>
> | 출처 | 1분 기여 | 비중 |
> |---|---|---|
> | 창 최저가 조건부 (**정의 인공물**) | 17.5bp | 11% |
> | 큰 하락이 있었다는 사실 | +112.7bp | 72% |
> | **그 하락이 청산성이라는 사실** | **+25.6bp** | **16%** |
>
> **1. 승률 99%는 정의 인공물이다.** 창의 최저가에 사면 창 안에서 손실이 구조적으로
> 어렵다 — $\text{close}[j{+}h] \geq \text{low}[j{+}h] \geq \text{low}[j] =$ 진입가.
> 무작위 시점도 81%다. **"승률 99% 전략"으로 읽으면 안 된다.**
>
> **2. 청산 고유 기여는 16%뿐이다.** $L(p)$ 를 완벽히 알아도 개선 대상은 156bp 가
> 아니라 그중 16%다. 상세는 `PROB_MODEL.md` §8.1.

### 2.4 왜 "1~5분" 인가

바닥에 진입하면 **되돌림의 61%가 첫 1분에** 들어온다(1분 156.1 / 15분 257.5 = 0.61).
3분이면 79%다. 되돌림은 빠르다.

비율 자체는 플라시보를 이긴다 — 무작위 시점은 17.5/51.2 = **34%**뿐이다. 즉 캐스케이드
뒤의 되돌림이 실제로 더 빠르다. 다만 **분자·분모 모두 §2.3 경고의 대상**이므로
"156bp 중 61%"가 아니라 "되돌림이 빠르다"는 정성적 사실만 취한다.

현행이 15분을 들고 있는 것은 되돌림이 느려서가 **아니라** 진입이 너무 높아서
남은 하락이 끝나기를 기다리는 것이다. 진입이 정확해지면 짧게 나오는 것이 맞다.

**주의**: 현행 진입(트리거 −2%)에서 1분 보유는 **−9.9bp로 손실**이다.
짧은 보유는 정확한 진입과 **묶여서만** 성립한다. 따로 떼어 적용하면 안 된다.

### 2.5 왜 "확률적 근사" 로 충분한가

바닥까지 거리 $X$ 의 실제 분포(21종 990이벤트):

| 분위 | 10% | 25% | 50% | 75% | 90% | 99% |
|---|---|---|---|---|---|---|
| $X$ | 0.14% | 0.41% | **1.15%** | 2.60% | 5.53% | 19.51% |

**고정값 하나로 맞출 수 있는 대상이 아니다.** 그러나 §2.3 표를 보면
바닥 ±0.5% 안에만 들어가도 1분 보유 106bp 이므로, **정확히 맞출 필요 없이
사건별로 대략의 위치만 잡아도 현행(88bp/15분)을 크게 넘는다.**

바닥 도달까지 걸리는 시간은 중앙 5분 / p90 49분이다.

---

## 3. 지금 이 설계를 막는 것 — 단 하나

| 필요한 것 | 있나 | 비고 |
|---|---|---|
| 가격대별 **오더북** $D_t(u)$ | **있다 (제약 있음)** | bookDepth ±1~5% 30초, 21종 1,300일 백필 완료. **단 라이브 `depth_poll`은 REST라 도달범위 BTC 0.19% / ETH 0.56%** |
| **호가 잔존율 $W$** | **없다** | 30초 스냅샷으로는 **취소를 못 본다**. §3.1 |
| 유량 (테이커 매수/매도) | **있다** | 1분봉, 6년 |
| 변동성·사건크기·쏠림(LSR/펀딩) | **있다** | 6년 |
| **가격대별 OI** $L(p)$ | **없다** | Hyperliquid `liquidationPx` 뿐이고 **과거 아카이브가 없다**. **단 들어갈 자리는 특정됐다** — 조건부 분포의 $\mu$ 하나 (`PROB_MODEL.md` §6) |

`liquidationPx` 는 라이브 쿼리에만 존재한다. 2026-07-31 부터 `hl_positions` 로
수집을 시작했고, 그 이전 구간은 **영구 결손**이다.

> ### ⚠ HL 실측 경로는 사실상 막혔다 (Q1, 2026-08-02)
>
> 44.7시간 × 실현청산 3,578건으로 1차 검정한 결과:
> **실현 청산이 일어난 가격대에서 지도가 비어 있는 비율 70.0%.**
> $\phi \approx 0.05$ 와 정확히 일치한다 — 시장의 5%만 보니 대부분의 청산은
> HL 에 포지션이 없는 가격대에서 일어난다.
>
> **이것은 표본 크기 문제가 아니라 구조적 커버리지 문제라 시간이 지나도 안 좋아진다.**
> 방향성 상관도 약하고 유의하지 않다(전체 +0.103 / 격리 +0.151, $n$=159).
> 단 **격리가 전체보다 낫다**는 방향은 가설과 맞는다.
>
> → **대안 A(§3.2)로 전환.** HL 은 지도가 아니라 **$f_R$ 보정용**으로 쓴다.

### 3.2 대안 A — 공개 데이터로 청산맵을 **재구성**한다

$$\hat L(p,t) = \sum_{\tau<t} \underbrace{\Delta OI^{+}(\tau)\,p(\tau)}_{\text{관측: 5분 OI + 가격}}
\times \underbrace{f_R\!\left(\tfrac{p}{p(\tau)}\right)}_{\text{청산거리 경험분포}}
\times \underbrace{S(t-\tau)}_{\text{생존}}$$

시각 $\tau$ 에 가격 $p(\tau)$ 에서 열린 물량이 $f_R$ 에 따라 청산가로 흩어지고,
그걸 과거 전체에 대해 더한 것이 현재의 지도다.

**교과서 공식 $p_{liq} = p_{entry}(1-1/\ell)$ 을 쓰지 않는다** — $|$오차$| < 10\%$ 가
**19.3%** 뿐이다(`lev_dist.py`, 유지증거금·펀딩 때문). $f_R$ **경험분포를 통째로** 쓰면
레버리지를 명시적으로 다룰 필요가 없고 그 문제를 우회한다.

**정규화**: $\sum_p \hat L(p,t) = OI(t) \times$ 격리비중. 이 제약이 $S$ 의 수준을 고정한다.

| 왜 이 경로인가 | |
|---|---|
| 바이낸스 자체 OI·가격을 쓴다 | **시장 전체를 본다** (HL 의 5%가 아니라) |
| 2020-09 까지 소급된다 | **6년 백테스트 가능** |
| $f_R$ 만 갈아끼우면 개선된다 | 초기 HL → 정본은 Bybit 실현청산 역산 |

**첫 결과** (`heatmap.py`, BTC 2026-07-30, 현재가 64,750):
현재가 −1% 안에 롱청산 연료 **\$10.5M**, 같은 구간 호가 \$20~40M → $V/D \approx 0.3$.

> Bybit 실현 클러스터로 잰 $V/D = 0.0065$ 와 **두 자릿수 다르다.**
> **지도는 "터질 수 있는 물량", 실현치는 "한 거래소에서 실제 터진 것"** 이다.
> 예보에 필요한 것은 전자다. (`PROB_MODEL.md` §9.15)

**A 의 최대 위험 — $f_R$ 이 바이낸스를 대표하는가.** HL 은 교차마진 94.5% 로
사용자층이 다르고, 코인별 형태가 뒤집혀 있다(HL 은 BTC/ETH 가 고레버리지 7.9~8.7%,
알트가 저레버리지 31~47% — 바이낸스 리테일은 반대일 가능성). **Bybit 역산이 정본.**

### 3.1 웹소켓 diff depth — $W$ 를 관측 가능하게 만드는 것

현행 `depth_poll` 은 REST 스냅샷이라 세 가지가 막힌다. `<symbol>@depth@100ms`
(diff stream) + REST 스냅샷으로 **로컬 북을 유지**하면 전부 풀린다.

| 한계 (REST 30초) | 웹소켓 diff |
|---|---|
| **도달범위** BTC 0.19% / ETH 0.56% — 그 너머 NaN | **전 깊이** |
| **30초 간격** — 캐스케이드가 몇 초에 끝난다 | **100ms** |
| **취소를 못 본다** — 스냅샷 차이만 보인다 | **호가 철수를 이벤트로 직접 관측** |

세 번째가 핵심이다. **$W$ 를 추정하는 게 아니라 실제로 보게 된다.**

**$L(p)$ 와 같은 성질이다 — 과거 백필이 불가능하고 지금부터 쌓아야 한다.**

구현 개요: REST `/fapi/v1/depth?limit=1000` 으로 초기 스냅샷 → diff 적용해 로컬 북
유지 → 1초마다 밴드 집계 저장. 21종이면 하루 약 180만 행.

**단 착수 순서가 있다** — QW(§4)를 기존 30초 데이터로 먼저 검정한다.
통과하면 이 수집기를 붙일 근거가 되고, 실패하면 붙일 이유가 없다.

거래량 프로파일로 $L(p)$ 를 복원하려는 시도는 구조적으로 교락된다
(`HAZARD_FINDINGS.md` §3: 복원값이 거래량 프로파일과 구별되지 않고, 거래량 노드는
정반대 효과(지지선)를 예측한다). 다만 그것은 **단변량 층화** 결과이므로,
오더북·유량과 함께 다변량으로 넣는 것은 아직 시도하지 않았다.

---

## 4. 검정 문항 (이 설계 기준)

| # | 문항 | 상태 |
|---|---|---|
| **A-1** | **재구성 지도가 실현 청산 위치를 맞히는가** | **다음 검정** |
| **A-2** | 재구성 지도로 $X$ 를 예보할 수 있는가 (**6년 백테스트**) | A-1 통과 후 |
| **Q1** | HL 청산맵이 시장 강제흐름의 **위치**를 대표하는가 | **사실상 불합격** — 70% 공백 (§3) |
| **QX-점** | 사전 정보로 $X$ 의 **점 추정**을 만들어 거기 걸면 되는가 | **실패** (2026-08-02) |
| **QX-분포** | 사전 정보로 $X$ 의 **조건부 분포**를 만들 수 있는가 ($D$ 상수 가정) | **실패** (2026-08-02) |
| **QW** | $W$(호가 잔존율)를 **사전 관측치로** 예보할 수 있는가 | **불합격** — 3단계. 예보 가능분이 이미 다른 특징에 포함 |
| **QΔ** | $\Delta(v,t)$(호가 추가/취소)의 분포를 모형화할 수 있는가 | 웹소켓 필요. 미착수 |
| **Q2b/Q3** | $\pi$ — 체결 상대가 정보 없는 강제매도일 확률 | **손도 안 댐.** `MECHANISM.md` §4 가 "유일한 경로"라 적어둔 항 |

> ### ⚠ 2026-08-02 저녁: 문제가 재정의됐다
>
> QX-분포가 떨어졌고 **원인이 진단됐다** — $D$ 를 관측 상수로 놓은 것.
> 화면의 호가는 확정 물량이 아니다. 캐스케이드 중 **109~523배 증발**한다.
>
> $$\log X = a + \gamma\log V - \gamma\log D_t \underbrace{-\ \gamma\log W}_{\text{빠져 있던 항}}$$
>
> 실측: 호가를 $V$ 만큼 먹었을 때 예측 도달점 $u_{pred}$ 중앙 0.018% 인데
> 실제 $u_{act}$ 중앙 0.223% — **9.2배**, 산포 [1.7, 13.8]. **그 산포가 $W$ 의 분포다.**
>
> **질문이 바뀐다**: ~~$V$ 를 예보할 수 있는가($L(p)$ 필요, 6개월 대기)~~ →
> **$W$ 를 예보할 수 있는가(30초 호가로 이미 관측 가능)**.
>
> 그리고 이쪽이 §1 설계의 "**실시간으로 대조**"와 더 맞는다 — 정적 $L(p)\times D$
> 대조가 아니라 **$D$ 가 무너지는 속도**를 보는 것이다.
> `PROB_MODEL.md` §9 · `LIQ_FINDINGS.md`
| **Q2c** | 가격대별 $\hat L(p)$ + 오더북을 **순차 갱신**하며 도달점을 추정 | Q1 통과 후 |
| **Q2b** | $\mathbb{E}[r\mid\text{fill}(u)]$ 가 $\hat L(u)$ 의 증가함수인가 | Q1 통과 후 |

> **QX-점 과 QX-분포 는 다른 문항이다.** `analysis/predict_x.py` 가 검정한 것은
> **QX-점** 이다 — $\log X$ 를 9개 특징에 OLS 회귀해 숫자 하나를 내고 배수를 곱했다.
> 표본 외 $R^2 = 0.009{\sim}0.029$, 짝지은 차이 27개 중 유의 1개(우연 기대치 1.35).
>
> **QX-분포 는 2026-08-02 에 검정했고 떨어졌다** (보정 4/9 위반, 판별 0.78 역방향).
> 원인은 $D$ 를 관측 상수로 놓은 것이다(`PROB_MODEL.md` §9).
> 원래 서술은 이랬다: 배치에 필요한 것은 $\mathbb{P}(X\geq u
> \mid \mathcal{F}_t)$ 라는 **함수 전체**이고, 점 추정에서는 그것이 나오지 않는다.
> 형태·합격 기준은 **`PROB_MODEL.md`** 를 볼 것. 합격 기준은 $R^2$ 가 아니라 **보정**이다.

**구조식은 확정됐다** (21종 639이벤트, 2023-01~2026-06, 표본 외 320):

$$\log X = a + \gamma\,\log\frac{V}{D}, \qquad \gamma = 0.875 \pm 0.078, \qquad R^2_{\text{표본외}} = +0.338$$

**독립 2회 검증**: 수익 회귀 0.875 / **오더북 모양 0.861** (적합 $R^2$ = 0.99).
데이터도 방법도 다른데 일치한다 — **함수 형태는 검증됐다.**

사전 관측치만으로는 $R^2 = 0.014$.

> **단 "예측력의 전부가 $V$ 에 있다"로 읽으면 안 된다** — $D$ 를 관측 상수로 놓았을
> 때만 그렇다. 실제 흡수 깊이는 $D_t \cdot W$ 이고 호가는 캐스케이드 중 변한다.
> 그리고 $V/D$ 자체가 **순차 흡수의 닫힌 해**이며, 가정 "$\Delta = 0$"이 틀렸다
> (`PROB_MODEL.md` §9.05).

**$V$ 를 사전에 모르는 것은 그대로다.** $L(p)$ 가 그것을 주는 물건인데
**HL 실측 경로는 70% 공백으로 막혔고**(§3), 지금은 **공개 데이터로 재구성**한다(§3.2).

---

## 5. 이미 시도해서 실패한 것 (같은 것을 반복하지 말 것)

**공통 원인: 전부 $\sigma$ 를 상수로 두거나 점 추정이었다 — 퍼짐을 모형화하지 않았다.**

| 시도 | 방법 | 결과 |
|---|---|---|
| Q2a `clearing.py` | 수급 청산 회계, 공급=$\Delta$OI **스칼라**, 일회 예측 | 사전 R²=−0.17. 상수(−0.11)보다 못함 |
| `adaptive_entry.py` | 오더북 두께 기반 손수 배치 규칙 | 표본 내 8위 / 표본 외 1위 — 불안정 |
| `hazard_model.py` | 조건부 해저드 $S(u\mid x)$ 적합 후 argmax EV. **$\sigma$ 상수** | 22.3bp < 무조건부 23.7 < 고정 24.2 |
| **`predict_x.py`** (2026-08-02) | $\log X$ 를 9개 특징에 **OLS 점 추정**, 예측값에 배수 | 표본 외 $R^2=0.009{\sim}0.029$. 짝지은 차이 27개 중 유의 1개 = 우연 기대치(1.35) |

세 번째가 특히 중요하다: **$S$ 를 잘 맞혀도 EV 는 안 오른다.** 무차별성이
$S \times \mathbb{E}[r|\text{fill}]$ **곱**에서 성립하므로 한쪽을 개선하면 다른 쪽이
상쇄한다. 단 **그 모형은 $\sigma$ 를 상수로 뒀다** — 사건 간 퍼짐 차이를 쓰지 않았다.

> ### ⚠ 이것을 "확률모델이 실패했다"로 읽지 말 것
>
> **실패한 것은 점 추정 배치다.** (확률모델 자체는 그 뒤 §4 QX-분포로 검정했고
> 역시 떨어졌다 — 단 원인이 $D$ 상수 가정으로 진단됐다.)
>
> - 점 추정 $\hat X = f(\mathcal{F})$ — **숫자 하나**. 배치에 못 쓴다
> - 확률모델 $\mathbb{P}(X \geq u \mid \mathcal{F})$ — **함수 전체**. 체결확률과
>   조건부 수익이 둘 다 여기서 나온다
>
> $\hat X$ 에 1.25를 곱한 것은 분위수가 아니고 아무 확률적 의미가 없다.
> 그리고 기존 무차별성($S(u)$ 평평)은 **무조건부** 곡선에서 나온 것이라
> **조건부 세계를 반증하지 않는다.** 정확한 형태와 합격 기준은 **`PROB_MODEL.md`**.

### 5.1 폐기된 제안

- **"가장 깊은 체결만 보유"** — 사다리는 위에서부터 순서대로 체결되므로, 가장 깊은
  것이 체결될 시점에 앞서 체결된 것들은 이미 물려 있다. 그때 정리하면 손실 확정이다.
  (2026-08-02 폐기)
- **손절** — −5% 손절이 이벤트당 EV 를 28.2 → −3.4bp 로 무너뜨린다. 좌측 꼬리는
  잘라낼 것이 아니라 되돌림을 기다려야 하는 구간이다.
- **짧은 보유만 따로 적용** — 현행 진입에서 1분 보유는 −9.9bp. §2.4 참조.

---

## 6. 현행 페이퍼는 이 설계가 아니다

`paper/baseline.py` 는 **고정 2% / 15분 / 손절 없음 / 무레버리지** 로 돌고 있다.
이것은 **기준선**이며, 위 설계가 존재 이유를 가지려면 이것을 이겨야 한다.

기준선 실측(21종 1,005이벤트): 체결률 32.2%, 조건부 +87.6bp, **이벤트당 28.2bp**,
t(NW) 3.8. 플라시보(CTRL) 대비 격차 35.2bp.

바꾸지 않는 이유: 위 설계를 구현할 입력($L(p)$)이 아직 없고, 스칼라 정보로 대체하려는
시도는 전부 실패했다(§5). 기준선을 먼저 실측해 둬야 나중에 비교가 된다.

---

## 7. 부수적으로 확정된 사실 (설계에 영향)

| 항목 | 실측 | 함의 |
|---|---|---|
| 청산 강도 $\|dOI\|$ | 연속값이 단조 신호. 표본 외 66.6 vs 40.3bp | 이진 임계보다 연속 가중이 낫다 |
| `dOI` 2% 임계 | 이벤트 $\|dOI\|$ 중앙 2.94%, 달러 중앙 \$3.5M | 너무 헐겁다. 다만 임계를 올리면 총량은 준다 |
| 청산 슬리피지 | \$500K 부근에서 수수료(7bp)를 넘음 | 최적 크기 \$2.5M/이벤트, 손익분기 \$5~10M |
| 심볼 편차 | \$1M 청산 슬리피지 BTC 0.9bp / 알트 20~22bp | 심볼별 균등 배분은 틀렸다 |
| 충격항 | $b\approx0.04$, 임계 없음 | 청산은 규모와 무관하게 가격을 거의 못 민다 |
| 무차별성 | 1차·2차 모멘트 모두 평평 (Sharpe 0.13) | $S$ 조건부화로는 못 깬다 |
