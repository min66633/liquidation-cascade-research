# 확률모델의 정확한 형태

**이 문서는 "확률모델"이 무엇인지 고정하는 정본이다.** 다른 문서와 충돌하면 이 문서가 맞다.
설계 자체는 `TARGET_DESIGN.md`, 측정된 것은 `MODEL.md`.

작성 2026-08-02 (같은 날 저녁 §4.5·§9 추가).
실측 근거의 데이터 기간: **21종 639이벤트, 2023-01-02 ~ 2026-06-17, 표본 외 320건**.
별도 표기가 없으면 전부 이 표본. (bookDepth 16종 백필 전에는 5종 252건이었고,
그 시기 수치는 각 항목에 표기했다.)

> **왜 이 문서가 필요한가**
> 2026-08-02 이전 연구에서 **점 추정을 확률모델이라 부른 오류**가 있었고,
> 그것이 "확률모델은 실패했다"는 잘못된 결론으로 이어질 뻔했다.
> §8 에 수정 목록이 있다.
>
> ### 현재 상태 (2026-08-02 저녁)
> - **구조식의 함수 형태 — 검증됨.** 독립 2회 ($\gamma$ = 0.875 수익회귀 / 0.861 오더북). §2
> - **확률화 (§5 형태) — 검정했고 떨어짐.** 보정·판별 둘 다 불합격. §4.5
> - **실패 원인 — 진단됨.** $D$ 를 관측 상수로 놓았다. 실제로는 $D_{\text{eff}} = D_t\cdot W$
>   이고 $1/W$ 는 중앙 3.4배 / p90 17배로 변동한다(극단일엔 109~523배). §9
> - **다음 문항 (QW) — 미검정.** $W$(호가 잔존율)를 예보할 수 있는가. §9.3

---

## 0. 표기 충돌 경고 — 먼저 읽을 것

| 기호 | 뜻 | 값 | 어디 |
|---|---|---|---|
| $b$ | **충격함수 지수** — 청산량이 가격을 미는 탄력성 | $\approx 0.04$ (사실상 0, **기각됨**) | `MODEL.md` 채널 A, `MATH.png` §10·§11 |
| $\gamma$ | **구조식 기울기** — $\log X$ 의 $\log(V/D)$ 탄력성 | $= \mathbf{0.875}$ (**살아있음**) | 이 문서 §2 |
| $\beta$ | 호가 프로파일 지수, $D(u) = Au^\beta$ | $\approx 0.14$ ($\gamma = 1/(1+\beta)$ 에서) | 양쪽 공통 |
| $W$ | **호가 잔존율** — 캐스케이드 중 실제로 남는 비율 | $1/W$ 중앙 **3.4배**, p90 17배 (극단일 109~523배) | 이 문서 §9 |

**$b$ 와 $\gamma$ 를 섞어 쓰지 말 것.** 둘은 다른 양이고 하나는 기각됐다.

---

## 1. 무엇이 확률변수인가

| 기호 | 뜻 | 관측 가능? |
|---|---|---|
| $X$ | 캐스케이드 바닥까지의 거리 (트리거 종가 대비) | **사후에만** |
| $V$ | 캐스케이드 중 진행방향 테이커 물량 (강제 + 재량) | **사후에만** |
| $D_t(u)$ | 현재가 ~ $u$ 구간 누적 대기물량 (**화면에 보이는 것**) | **관측됨** (30초) |
| **$W$** | **호가 잔존율** — $D_t$ 중 실제로 먹을 수 있는 비율 | **부분 관측** — 30초 스냅샷의 $D_{min}/D_{pre}$ 는 **소진과 철수가 섞인** 값. 순수 철수분은 웹소켓 diff 로만 분리 ← §9 |
| $L(p)$ | 가격 $p$ 에 걸린 강제청산 예정물량 (히트맵) | **없음** — 2026-07-31 부터 수집 중 |

> **§5 모델은 $V$ 만 확률변수로 놓았다. 그게 실패의 원인이다** — $D_t$ 는 관측되지만
> **먹을 수 있는 깊이는 $D_t \cdot W$ 이고 $W$ 는 미관측**이다. §9.

---

## 2. 구조식 (실측으로 확정 — **독립 2회 검증**)

$$\log X \;=\; a + \gamma \log\frac{V}{D}$$

**유도**: 깊이 밀도 $D(v) = Av^\beta$ 를 흡수하며 밀린다면 $\int_0^X D(v)dv = V$ 에서
$X = ((1+\beta)V/A)^{1/(1+\beta)}$, 즉 $\gamma = 1/(1+\beta) \in (0,1]$.

| 방법 | $\gamma$ | 데이터 |
|---|---|---|
| **수익 회귀** | **0.875 ± 0.078** ($t$=11.2, 일자클러스터) | 21종 639이벤트, 2023-01~2026-06 |
| **오더북 모양** | **0.861** (적합 $R^2$ = 0.99) | 27클러스터, Bybit×depth_poll 40시간 |

$\text{Cum}(u) = Bu^\kappa$ 를 호가 밴드에 적합해 $\gamma = 1/\kappa$ 를 얻는다.
**데이터도 방법도 완전히 다른데 0.875 vs 0.861 이다.**

$\Rightarrow$ **구조식의 함수 형태는 검증됐다.** ($\gamma=1.011$ 은 5종 판이었고
21종에서 0.875 로 정정됐다. $\beta \approx 0.14$.)

표본 외 $R^2 = +0.338$ (21종, 창 길이 순환 제거, 60분 고정창).

같은 표본에서 **사전 관측치만으로는 $R^2 = +0.014$**.

$$\underbrace{0.014}_{\text{사전 관측}} \quad\longrightarrow\quad \underbrace{0.338}_{V \text{를 사후에 알 때}}$$

### 2.1 한계 — 역인과

$V$ 는 강제매도 + 재량매도 합계이고, "가격이 밀려서 매도가 나왔다"는 역방향 인과가
섞여 있다. `vd_structure.py` 의 부호 제약 검정은 **예측과 반대**로 나왔다 —
강제분 비중 상위 3분위에서 $R^2$ 가 **가장 낮았다**(0.085 vs 중간층 0.165).
다만 그 층화가 $\lvert dOI\rvert$ 기반이라 **자발적 종료를 강제분으로 오인**한다.
`VD_FINDINGS.md` §4.

### 2.2 이 $\Rightarrow$ 를 "예측력의 전부가 $V$ 에 있다"로 읽으면 안 된다

$D$ 를 관측 상수로 놓았을 때만 그렇다. §9 가 맞다면 잔여 분산의 상당부분이
$W$ 에 있고, 그것은 $V$ 를 완벽히 알아도 남는다.

---

## 3. 무조건부 $S(u)$ 와 조건부 $\mathbb{P}(X \geq u \mid \mathcal{F}_t)$ — 이 구분이 전부다

기존 문서(`MATH.png` §2, `MODEL.md`, `EXPLAINER.md` §6·§7)의 확률은 **전부 무조건부**다:

$$S(u) = \mathbb{P}(X \geq u) \qquad \text{— 사건을 구분하지 않는 \textbf{하나}의 곡선}$$

여기서 무차별성($S(u)\cdot\mathbb{E}[r\mid\text{fill}] \approx 46\text{–}52$bp 평평)이 나왔고,
그래서 "어디 걸어도 같다"는 결론이 나왔다. **그 결론은 무조건부 세계에서만 참이다.**

설계가 요구하는 것은 사건마다 다른 곡선이다:

$$\mathbb{P}(X \geq u \mid \mathcal{F}_t) \qquad \text{— 사건 } t \text{ 마다 \textbf{다른} 곡선}$$

무조건부 곡선이 평평해도 **사건별 곡선이 서로 다르면** 사건별로 다른 $u$ 를 고를 수 있고
그때 EV 는 평평하지 않을 수 있다.

> **무차별성은 조건부 세계를 반증하지 않는다.** 조건부를 한 번도 검정하지 않았을 뿐이다.
>
> "확률모델은 이미 있다"는 오해가 여기서 나왔다. $S(u)$ 가 확률이긴 하지만
> **무조건부**라서 배치 결정에 쓸 수 없다. 배치는 "**이 사건에서**" 어디에 걸지를 정하는 것이다.

---

## 4. 점 추정 vs 확률모델

|  | 형태 | 배치에 쓸 수 있나 |
|---|---|---|
| **점 추정** | $\hat X = f(\mathcal{F})$ — **숫자 하나** | **아니오** |
| **확률모델** | $F(x \mid \mathcal{F}) = \mathbb{P}(X \leq x \mid \mathcal{F})$ — **함수 전체** | 예 |

배치의 기대값은

$$\mathrm{EV}(u) \;=\; \underbrace{\mathbb{P}(X \geq u \mid \mathcal{F})}_{\text{체결확률}}\;\cdot\;
\underbrace{\mathbb{E}\big[r \mid X \geq u,\ \mathcal{F}\big]}_{\text{체결 시 수익}}\;-\;c$$

**두 항 모두 분포에서 나온다. 점 추정에서는 어느 쪽도 나오지 않는다.**
$\hat X$ 에 0.75나 1.25를 곱하는 것은 분위수가 아니며 아무 확률적 의미가 없다.

`analysis/predict_x.py` 가 한 것이 정확히 후자다 — $\log X$ 를 9개 특징에 OLS 회귀해
숫자 하나를 내고 거기에 배수를 곱했다. 그것이 실패한 것이지 확률모델이 실패한 게 아니다.

---

## 4.5 ⚠ 아래 §5 모델은 검정했고 떨어졌다 — 원인은 §9

**2026-08-02, `x_dist.py`, 21종 639이벤트, 평가 320:**

| 관문 | 결과 |
|---|---|
| 보정 | **불합격** — Kupiec 9개 중 4개 위반. 9개 $p$ 전부 실제 < 명목(한 방향 계통오차) |
| PIT KS | **기각** $p=0.029$. 무조건부판(M0)은 $p=0.243$ 통과 |
| 판별 | **불합격** — 예측 $\sigma$ 3분위 → 실제 $\log X$ IQR 비 **0.78 (역방향)** |

**단 구조식의 함수 형태는 살아 있다** — §2 참조, 독립 2회 검증.
실패한 것은 **확률화 방식**이고, 원인은 **$D$ 를 관측 상수로 놓은 것**이다(§9).

---

## 5. 모델의 형태 (실패판 — 기록용)

$D$ 는 관측되므로 불확실성을 전부 $V$ 로 몰아넣는다.

$$\log V \;\big|\; \mathcal{F}_t \;\sim\; \mathcal{N}\big(\;\mu(\mathcal{F}_t),\;\; \sigma^2(\mathcal{F}_t)\;\big)$$

> ### 핵심: $\mu$ 와 $\sigma$ 를 **둘 다** 모형화한다.
> $\sigma$ 를 상수로 두면 그건 다시 점 추정이다. 사건마다 **퍼짐이 다르다**는 것이
> 확률모델의 존재 이유다 — 어떤 캐스케이드는 바닥이 뻔하고 어떤 건 아무데나 간다.

구조식으로 밀면 닫힌 형태가 나온다:

$$\mathbb{P}\big(X \geq u \mid \mathcal{F}_t\big)
\;=\; \Phi\!\left(\frac{\mu(\mathcal{F}_t) \;-\; \log D_t \;-\; (\log u - a)/\gamma}
{\sigma(\mathcal{F}_t)}\right)$$

$\mu(\mathcal{F}_t) = w_\mu^\top x_t$, $\;\log\sigma(\mathcal{F}_t) = w_\sigma^\top x_t$ 로
각각 선형 적합한다 (이분산 명시).

---

## 6. $L(p)$ 가 들어가는 자리 — 정확히 한 곳

$$\mu(\mathcal{F}_t) \;\longleftarrow\;
\log\Big[\underbrace{\textstyle\int_{p_t}^{u} L(p)\,dp}_{\text{강제분 — 히트맵}}
\;+\; \underbrace{M(u)}_{\text{재량분}}\Big]$$

**히트맵은 $\mu$ 를 준다. 그것뿐이다.**
$\sigma$ 는 커버리지 $\phi \approx 0.05$ 와 재량분 $M$ 의 불확실성에서 온다.
구조식도, 보정 검정도, 배치 규칙도 **아무것도 바뀌지 않는다.**

그래서 지금 $L(p)$ 없이 만들어도 나중에 **그 함수 하나만 갈아끼우면** 된다.

### 6.1 완전판 (자기참조 형태, $L(p)$ 도착 후)

$u$ 까지 밀리려면 $u$ 까지의 강제물량을 흡수해야 하는데, 흡수 과정이 다시 더 깊은
청산을 촉발한다. 도달점은 고정점이다:

$$u^* \;=\; \inf\big\{\,u \;:\; D(u) \;\geq\; V(u)\,\big\},
\qquad V(u) = \int_{p_0}^{u} L(p)\,dp \;+\; M(u)$$

$L$ 이 시장의 5% 표본이고 $D$ 가 실시간으로 다시 차므로 $u^*$ 는 **확률변수**다.
이것이 목표 설계 — "가격대별 OI와 오더북을 실시간 대조해 바닥을 확률적으로 근사" —
의 수식 형태다.

---

## 6.5 ⚠ 이 삽입점 자체가 재검토 대상이다

§6 은 "$L(p)$ 가 $\mu$ 를 준다"로 끝난다. 그런데 §9 가 맞다면 지배적 불확실성은
$V$ 가 아니라 $W$ 이고, $L(p)$ 는 $W$ 에 대해 아무것도 말해주지 않는다.

**$L(p)$ 의 가치는 §9 의 QW 검정 결과에 종속된다.**

---

## 7. 합격 기준 — $R^2$ 가 아닌 이유

$R^2$ 는 $\mu$ 만 잰다. 그런데 배치에 실제로 쓰이는 것은 **분위수**이고, 분위수는
$\sigma$ 가 정한다. **$R^2$ 가 낮아도 $\sigma$ 가 맞으면 확률모델은 작동한다.**

| 층 | 기준 | 뜻 |
|---|---|---|
| **① 보정** | 9개 분위 전부 실제-명목 차이 ±5%p, Kupiec $p > 0.05$ | "$\mathbb{P}(X\geq u) = 0.3$" 이라 했을 때 실제로 30%가 체결되는가 |
| **② 판별** | 조건부 IQR 상/하위 3분위의 실제 IQR 비 ≥ 1.5 | **넓다고 예측한 사건이 실제로 넓은가** |
| **③ 경제** | 짝지은 차이가 Bonferroni 보정 후 유의 | 고정 offset 을 실제로 이기는가 |

**순서대로** 통과해야 한다. 보정이 깨지면 ③은 의미가 없다.
사전 등록이며 **사후 변경 금지**(`.claude/plans/plan-20260802-x-prob-model.md` §5).

---

## 8. 2026-08-02 이전 연구의 수정 목록

| # | 무엇이 틀렸나 | 정정 |
|---|---|---|
| **1** | `analysis/predict_x.py` 를 "확률모델 검정"으로 서술 | **OLS 점 추정**이다. 분포가 없다 |
| **2** | "QX 실패 = 확률모델 실패" | 당시엔 **"점 추정 배치가 실패"** 가 정확했다. 확률모델(§5)은 그 뒤 검정했고 **역시 떨어졌다** — 원인은 $D$ 상수 가정(§9) |
| **3** | `TARGET_DESIGN.md` §2.3 "완벽예지 바닥 +156bp" | 그중 **청산 고유 기여는 16%**. §8.1 참조 |
| **4** | 같은 곳 "승률 99%" | **정의 인공물**. 무작위 시점도 81% |
| **5** | 소진감지 "바닥 前 진입 17%" | **룩어헤드**. 사후 확정된 60분 창 최저점 기준. 창길이 재측정 전까지 **인용 금지** |
| **6** | `TARGET_DESIGN.md` §5 `hazard_model.py` 항목 | "$S$ 를 잘 맞혀도 EV 는 안 오른다"는 맞다. 단 **그 모형은 $\sigma$ 를 상수로 뒀다** |
| **7** | 표기 | 구조식 기울기를 $b$ 로 쓰면 충격지수 $b\approx0.04$(기각)와 충돌. **$\gamma$ 로 고정** (§0) |
| **8** | `MATH.png` §2 가 $S(u)$ 를 무조건부로만 정의 | 문서 전체가 무조건부 위에 서 있다. **오해의 근원** (§3) |

### 8.1 완벽예지 +156bp 의 분해

**21종 990이벤트, 2020-09~2026-06 / 플라시보는 21종 1,005 vs 8,400 vs 2,109, 60분 창, 7bp 차감**

| 표본 | n | 1분bp | 3분 | 15분 | 승률(1분) |
|---|---|---|---|---|---|
| 청산 이벤트 | 1,005 | 155.8 | 202.2 | 256.8 | **99.0%** |
| 무작위 시점 | 8,400 | **17.5** | 27.6 | 51.2 | **81.0%** |
| 같은 하락, OI 유지 | 2,109 | **130.2** | 167.7 | 248.7 | — |

| 출처 | 1분 기여 | 비중 |
|---|---|---|
| 창 최저가 조건부 (**정의 인공물**) | 17.5bp | 11% |
| 큰 하락이 있었다는 사실 | +112.7bp | 72% |
| **그 하락이 청산성이라는 사실** | **+25.6bp** | **16%** |

두 가지가 나온다.
1. **승률 99%는 거의 전부 정의 때문이다.** 창의 최저가에 사면 창 안에서 손실이
   구조적으로 어렵다($\text{close}[j{+}h] \geq \text{low}[j{+}h] \geq \text{low}[j] = $ 진입가).
   무작위 시점도 81%다.
2. **청산 고유 기여는 16%뿐이다.** $L(p)$ 를 완벽히 알아도 개선 대상은 156bp 가 아니라
   그중 16%다.

다만 **배치 정밀도의 민감도**(바닥 ±0.5%면 106bp, ±2%면 −44bp)는 그대로 살아 있다.
그건 플라시보와 무관한 별개의 측정이다.

### 8.2 살아남는 것

- 구조식 $\log X = a + \gamma\log(V/D)$, **$\gamma = 0.875 \pm 0.078$**, 표본 외 $R^2 = 0.338$
  (21종. 오더북 모양 독립 추정 0.861 과 일치)
- 사전 관측치 무력 $R^2 = 0.014$ — 단 **"$V$ 가 전부"는 아니다** (§2.2, §9)
- 무차별성 (1차·2차 모멘트 평평, Sharpe 0.13) — **단 전부 무조건부·$\sigma$ 상수 모형**
- 붕괴 깊이의 사건 선별력 (조건부 EV 22.0 → 113.2bp) — 단 **선별인지 타이밍인지 미분리**
- $\lvert dOI \rvert$ 연속 가중 (표본 외 66.6 vs 40.3bp)
- 기준선 페이퍼 실측 (이벤트당 28.2bp, t(NW)=3.8)

### 8.3 `TARGET_DESIGN.md` §5 실패 목록의 재해석

세 시도(`clearing.py`, `adaptive_entry.py`, `hazard_model.py`)는 **전부 $\sigma$ 를
상수로 두거나 점 추정이었다.** "스칼라로 바닥을 맞히려 했다"는 기존 진단은 맞지만,
더 정확한 공통 원인은 **퍼짐을 모형화하지 않았다**는 것이다.

이것은 실패 목록을 지우자는 뜻이 **아니다.** 같은 실패를 반복하지 않으려면
새 모델이 반드시 $\sigma(\mathcal{F})$ 를 갖고 §7 의 ①②를 통과해야 한다는 뜻이다.

---

## 9. $D$ 는 관측값이 아니라 확률변수다 (2026-08-02) — §5 실패의 원인

§5 모델은 $D$ 를 **관측된 상수**로 놓고 불확실성을 전부 $V$ 로 몰았다. 그게 틀렸다.

화면의 호가는 확정 물량이 아니다. 마켓메이커·봇이 넣었다 뺐다 한다.
실측: 청산 이벤트 중 1% 이내 호가가 **중앙 3.4배 / p90 17배** 얇아지고,
극단일에는 109~523배까지 간다(`DEPTH_FINDINGS.md` 는 후자를 보고한 것이다).

$$\log X \;=\; a + \gamma\big(\log V - \log D_{\text{eff}}\big),
\qquad D_{\text{eff}} = \underbrace{D_t}_{\text{관측}} \cdot \underbrace{W}_{\text{잔존율, 미관측}}$$

$$\log X = a + \gamma\log V - \gamma\log D_t \underbrace{-\ \gamma\log W}_{\textbf{빠져 있던 항}}$$

$$\sigma^2 = \gamma^2\Big(\underbrace{\sigma^2_{\log V}}_{\text{모형화한 것}}
+ \underbrace{\sigma^2_{\log W}}_{\textbf{무시한 것}} - 2\,\mathrm{Cov}\Big)$$

### 9.05 나누기 형태가 무엇을 숨기는가

물리는 **순차 흡수**다. 가격은 가까운 호가부터 차례로 먹으며 내려간다:

$$X = \inf\Big\{u : \int_0^u \big[\underbrace{d_{\text{rest}}(v)}_{\text{깔린 호가}}
+ \underbrace{\Delta(v,t)}_{\text{MM 추가/취소}}\big]\,dv \;\geq\; V\Big\}$$

$V/D$ 는 이 식의 **닫힌 해**이며, 두 가정 위에 선다:

1. 호가 모양이 멱함수 $d_{\text{rest}}(v) = Av^\beta$ → **검증됨** (적합 $R^2$ = 0.99, §2)
2. $\Delta = 0$ (캐스케이드 중 호가가 안 변한다) → **틀림**

$\gamma$ 가 두 방법에서 일치한 것은 **가정 1 이 맞다**는 증거다.
가정 2 를 뒤늦게 $W$ 로 넣었는데, **스칼라 $W$ 는 조잡하다** —
실제 대상은 $\Delta(v,t)$, 즉 **가격대마다 다르고 시간에 따라 변하는 함수**다.
MM 이 근접 호가만 빼고 깊은 것은 둘 수도, 반대일 수도 있다.

> **모형화해야 할 확률적 대상은 $\Delta(v,t)$ 다.**
> 틱 단위 예측은 필요 없다 — 캐스케이드 창 전체에 걸친 **분포**면 된다.
> 그건 100ms 웹소켓 diff 로 관측된다(`TARGET_DESIGN.md` §3.1).

**$1/W$ 실측 분포** (`w_survival.py`, 21종 636이벤트, 2023-01~2026-06):

| | p10 | 중앙 | p90 | 극단일 |
|---|---|---|---|---|
| $W_{min}$ | 0.059 | **0.294** | 0.651 | — |
| $1/W_{min}$ | 17.0배 | **3.4배** | 1.5배 | 109~523배 |

> **정정 (2026-08-02 심야)**: 이 문서와 `MATH.png` 가 "$1/W \in [109, 523]$" 이라고
> 쓴 것은 **극단일(2025-10-10) 수치를 전체 범위처럼 인용한 것**이었다.
> 전형적인 붕괴는 **3.4배**이고 p90 이 17배다. 109~523배는 꼬리다.

$W_{pre}$(트리거 깊이 / 직전 60분 중앙) 중앙 **0.997** — **트리거 시점 자체는 얇지 않다.**
붕괴는 트리거 **이후**에 일어난다.

### 9.1 이 한 가지가 관측된 실패 넷을 전부 설명한다

| 실패 | 설명 |
|---|---|
| $\sigma$ 모형이 **역방향**(0.78) | 지배적 불확실성이 $\log W$ 인데 사전 특징에 그 정보가 없다 |
| 보정이 **한 방향으로** 깨짐 | $\log W$ 항을 빼먹었으니 위치가 계통적으로 어긋난다 |
| $u_{act}/u_{pred}$ 중앙 **9.2배** | ~~그 산포가 곧 $W$ 의 분포다~~ → **§9.15 정정** |
| $V/D_t$ 가 0.65% 로 작아 보임 | ~~분모가 허수다~~ → **§9.15 정정** |

### 9.15 ⚠ 정정 — 9.2배를 $W$ 로 귀속한 것은 과잉주장이었다

`liq_cluster.py` 에서 $u_{pred}$ 를 계산할 때 쓴 $V$ 는 **Bybit 청산만**이다. 그런데

- Bybit 는 perp 시장의 **10~15%**
- 청산은 전체 테이커 물량의 **~10%** (`|dOI|` 대비 $V_{60}$ 비교에서)

즉 **실제로 호가를 먹은 물량은 내가 쓴 $V$ 보다 한두 자릿수 크다.**
$u_{act}/u_{pred} = 9.2$배는 **호가 철수($W<1$)와 $V$ 과소측정이 섞인 값**이고,
나는 전부 $W$ 탓으로 돌렸다. **그 귀속은 성립하지 않는다.**

같은 이유로 "$V/D_t = 0.65\%$ 라 분모가 허수다" 도 성립하지 않는다 —
분자가 과소측정된 것일 수 있다. 재구성 히트맵(`heatmap.py`)으로 **서 있는 연료**를
재면 BTC 기준 현재가 ±1% 안에 \$10.5M 이고, 같은 구간 호가가 \$20~40M 이라
$V/D \approx 0.3$ 이다 — **두 자릿수 다르다.**

> **지도는 "터질 수 있는 물량", Bybit 실현치는 "한 거래소에서 실제 터진 것".**
> 예보에 필요한 것은 전자다.

**$\gamma$ 만 두 방법에서 일치한 것도 일관된다** — $W$ 는 곱셈 상수라 기울기를 안
건드리고 절편과 분산만 옮긴다.

### 9.2 근거 (`analysis/liq_cluster.py`, Bybit 전건 39.9시간, 클러스터 27개, 2026-07-31~08-02)

| 항목 | 값 |
|---|---|
| $V/D$(도달 최심) | 중앙 **0.0065**, p90 0.035, 최대 0.076 |
| $u_{pred}$ (호가를 $V$만큼 먹으면) | 중앙 **0.018%** |
| $u_{act}$ (실제 10분 최대 역행) | 중앙 **0.223%** |
| 배수 | 중앙 **9.2배** [p25 1.7, p75 13.8] |
| $\mathrm{corr}(\log(V/D), \log u_{act})$ | **−0.045** ($n$=21) |

**과잉해석 금지 — 세 가지 한계**

1. **이 40시간에 캐스케이드가 없었다** (최대 클러스터 \$2.26M). $V/D$ 범위가 좁아
   상관 0 은 "관계 없음"이 아니라 **범위 제약**이다. $n$=21 에서 SE≈0.24 라
   $\lvert dOI\rvert$ 판의 +0.224 를 배제하지 못한다.
2. **Bybit 한 거래소분**이다. perp 점유율 10~15% 이므로 전 거래소 합산이면
   $V/D$ 는 **5~6%** 일 수 있다. 여전히 작지만 0.65% 만큼 극단은 아니다.
3. 거래소 교차(Bybit 청산 × Binance 호가·가격), 롱청산 편중,
   BTC/ETH 는 REST 도달범위(0.19%/0.56%) 때문에 대부분 탈락 → 알트 편중.

### 9.25 $W$ 는 잡음이 아니다 — $L$ 과 얽혀 있다

위 §9 는 $W$ 를 **외생 잡음**으로 놓았다. 그런데 시장에 **OI가 두꺼운 쪽을 터뜨리려는
유인**이 있다면 $W$ 와 $L$ 은 독립이 아니다:

$$\log X = a + \gamma\Big(\log \underbrace{V(u)}_{\textstyle\int L(p)dp}
- \log D_t - \log \underbrace{W(u)}_{L(u)\text{의 감소함수}}\Big)$$

**$L$ 이 두꺼운 곳에서 물량이 더 쏟아지고 동시에 호가가 더 사라진다. 곱해진다.**

이것이 $V/D_t = 0.65\%$ 인데 가격이 **9.2배** 더 밀리는 이유의 유력한 후보이고,
$\sigma$ 모형이 실패한 이유이기도 하다 — $W$ 의 변동이 $L$ 에 조건부인데
내 사전 특징에 $L$ 이 없었으니 잡힐 리가 없다.

**문헌**: Brunnermeier & Pedersen (2005) *Predatory Trading*; Osler (2005) FX 스탑 캐스케이드.
둘 다 이미 참고문헌에 있다.

**비판적 단서**: MM 은 스프레드로 벌지 방향으로 벌지 않으므로 "터뜨린다"는 **의도**는
MM 보다 방향성 트레이더에 더 맞는다. MM 쪽은 **역선택 회피**로 호가를 뺀다.
그런데 두 이야기의 **관측 예측이 같다**(OI 두꺼운 쪽 호가가 사라진다).
어느 쪽이 맞는지 몰라도 검정은 성립한다.

**따라서 §6.5 의 우려가 뒤집힌다** — $L(p)$ 는 $V$ 만이 아니라 $W$ 도 예보한다.
곱해지므로 오히려 더 강해진다. 단 **미검정**이며 $L(p)$ 와 전 깊이 북이 둘 다
쌓여야 검정할 수 있다.

### 9.3 질문이 바뀐다

| | 질문 | 필요한 것 |
|---|---|---|
| 이전 | $V$(강제매도량)를 예보할 수 있는가 | $L(p)$ — **아카이브 없음, 6개월 대기** |
| **지금 (QW)** | **$W$(호가 잔존율)를 예보할 수 있는가** | **30초 호가 — 이미 있다** |

그리고 이쪽이 목표 설계의 "**실시간으로 오더북을 대조한다**"와 더 맞는다.
정적 $L(p) \times D$ 대조가 아니라 **$D$ 가 무너지는 속도**를 보는 것이다.

**미검정.** 이것이 다음 문항이다.
