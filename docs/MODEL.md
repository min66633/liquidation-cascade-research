> ## ⚠ 먼저 읽을 것 — `TARGET_DESIGN.md`
>
> 이 문서는 **측정된 것**의 정본이고, 만들려는 것의 정본은 `TARGET_DESIGN.md` 다.
> 충돌하면 그쪽이 맞다.
>
> **목표 설계**: 가격대별 OI 히트맵과 가격대별 오더북을 실시간 대조해
> **캐스케이드 바닥을 확률적으로 근사**하고 거기 지정매수를 건 뒤 **1~5분 짧게 보유**.
>
> **근거**: 바닥에서 1%만 벗어나도 수익의 2/3가 사라진다(1분 156 → 56bp).
> 현행 트리거 −2% 진입은 1분 **−9.9bp** / 15분 +88bp. 진입 정밀도가 전부다.
> ~~완벽 예지 1분 +156bp(승률 99%)~~ — **이 절대수치는 2026-08-02 플라시보로
> 대부분 기각됐다.** 청산 고유 기여는 16%뿐이고 승률 99%는 정의 인공물이다
> (`TARGET_DESIGN.md` §2.3, `PROB_MODEL.md` §8.1). 살아남는 것은 **민감도**다.
>
> **막는 것**: 가격대별 OI(=$L(p)$)가 과거 데이터에 없다. HL `liquidationPx` 뿐이고
> 2026-07-31 부터 수집 시작. 그 이전은 영구 결손. **단 들어갈 자리는 특정됐다** —
> 조건부 분포의 $\mu$ 하나 (`PROB_MODEL.md` §6).
>
> **현행 페이퍼(고정 2%/15분)는 이 설계가 아니라 넘어야 할 기준선이다.**

> ## ⚠ 이 문서의 확률은 전부 **무조건부**다
>
> 아래 §5 의 $S(u) = \mathbb{P}(X \geq u)$ 는 **사건을 구분하지 않는 하나의 곡선**이고,
> §6 의 EV 와 §7 의 무차별성이 전부 그 위에 서 있다.
>
> **설계가 요구하는 것은 조건부** $\mathbb{P}(X \geq u \mid \mathcal{F}_t)$ —
> **사건마다 다른 곡선**이다. 무조건부 곡선이 평평해도 사건별 곡선이 서로 다르면
> 사건별로 다른 $u$ 를 고를 수 있다.
>
> **무차별성은 조건부 세계를 반증하지 않는다.** 조건부를 한 번도 검정하지 않았을 뿐이다.
> 정확한 형태와 합격 기준은 **`PROB_MODEL.md`**.

# 청산 캐스케이드 확률모델 (v3) — 무조건부

개정 2026-08-01. v2 대비 바뀐 것:
**(a) 임계형 충격항을 폐기한다** — 지지집합을 42배 넓혀 검정했고 꺾임의 증거가 없다.
**(b) $D$ 를 상수에서 과정으로 승격한다** — 캐스케이드에서 순간 122~523배 증발한다.
**(c) §4 의 출처 오류를 정정한다** — 인용 수치는 taker 연구였는데 maker 로 적혀 있었다.
각 모수에 측정치인지 가정인지를 명시한다.

---

## 0. v1의 충격항 — 이제는 "미검정"이 아니라 **임계형이 반증됐다**

v1은 청산이 가격을 민다고 놓았다: $dM = \kappa L(u)^{\beta}du - \lambda M du$.
v2는 이를 임계형 $\max(0, V/D-c)^\beta$ 로 바꿔 "$c>0.31$, 그 위는 미검정"으로 뒀다.

**그 0.31 이 6일치 표본에서 나온 값이었다** (`impact_depth.py`, 실제 일수 확인 결과):

| | |
|---|---|
| 표본 일수 | **6일** (2026-02-01 · 03-01 · 04-01 · 05-01 · 06-01 · 07-01) |
| 그 6일의 max\|z\| 백분위 중앙값 | **52.2%** — 정확히 평범한 날 |
| 그 6일의 청산성 이벤트(8σ+dOI≤−2%) | **0건** |

전 기간 1,307일 중 청산성 이벤트가 있는 날은 148일(11.3%)이고, 6일이 전부 비껴갈
확률은 $0.887^6\approx49\%$ 다.

> **1,423버스트 표본에는 이 전략이 거래하는 사건이 한 건도 없었다.** "임계 미달"이
> 아니라 **사건 부재**다. $\max(0,V/D-c)$ 가 표본 전체에서 항등적으로 0 이면
> $\kappa,\beta,c$ 는 우도에 들어가지 못한다 — 지지집합 문제이며 **같은 날들에서
> 관측을 늘려도 해결되지 않는다.** 필요한 것은 관측 수가 아니라 **다른 날**이었다.

### 다시 재고 나서 (`depth_collapse.py`, 5종 183일, 청산성 267건, 2023-01~2026-07)

bookDepth 는 2023-01 이후 전 일자가 무료다. 청산성 이벤트가 있던 148일을 추가로 받았다.

| | v2 | v3 |
|---|---|---|
| $V/D$ 지지집합 | ≤ 0.31 | 청산이벤트 **88.3%가 0.31 초과**, 최대 상한의 **42배** |
| 형태 | 임계형(볼록) 가정 | **꺾임 없음** — 완만한 단조 증가. $V/D$ 30배당 변위 2.8배 |
| $\beta$ | 미추정 | $b\approx0.04$ (전체). 큰 사건에서는 **부호 미확정** |

$V/D$ 가 100배 커져도 변위는 1.22배다. 제곱근 법칙(0.5)의 1/11 이다.
전체 표본 t=8.04(일자 클러스터)는 11만 바가 만든 **통계적** 유의성이며 경제적
유의성이 아니다. iid 표준오차를 쓰면 t=15.95 로 2배 부풀려진다 — 5분봉인데 종속변수가
15분 전방창이라 이웃끼리 겹치고 5종이 같이 움직이기 때문이다.

**벤더 결함 주의**: bookDepth 는 2025년 구간에서 특정 밴드가 몇 시간씩 고정된다
(2025-10-10 BTC `-1%` 는 동일 notional 이 440회 연속). 안 거르면 $V/D$ 꼬리가
통째로 날조된다 — 이 문서 초판은 그래서 "2,471배"라 적었다. 필터는
`analysis/bookdepth.py`, 비용은 0.8~2.6%다.

**"기각"이라고 쓰지 않는 이유**: (a) $V$ 대용치(OI 급감액)가 자발적 청산을 포함해
감쇠편의로 계수를 0 쪽으로 민다. (b) 고정값을 걸러내면 $V/D \ge 10$ 구간이 **n=31**
로 비어, 임계가 있다면 있을 만한 곳을 여전히 못 본다. 정확한 상태는
**"꺾임의 증거가 없고 잔여 효과는 경제적으로 무의미하다"** 이다.

상세: `analysis/DEPTH_FINDINGS.md`

---

## 1. 좌표계

$$u \;=\; 1 - p/p_0 \quad (\ge 0)$$

가격축을 따라 정의한다. 5분봉으로 재면 캐스케이드 하나가 관측점 2~3개로 뭉개진다(실측).

---

## 2. 정보 없는 매도 흐름 밀도 $U(u)$

되돌림의 원천은 충격 크기가 아니라 **정보 없는(uninformed) 강제 흐름**이다.
그 흐름은 두 층으로 이루어진다.

$$\boxed{\;U(u) \;=\; \underbrace{L(u)}_{\text{강제청산}} \;+\; \underbrace{\Sigma(u)}_{\text{재량 손절}}\;}$$

| | 관측 | 근거 |
|---|---|---|
| $L(u)$ 강제청산 밀도 | **가능** — HL `liquidationPx` × 명목가 | 실측 지도 |
| $\Sigma(u)$ 재량 손절 밀도 | **불가능** — 어디에도 기록 없음 | Osler(2005) FX 스탑 캐스케이드 |

**두 층이 구조적으로 분리된다** (실측, 청산가 보유 50,551건):

| 현재가 대비 거리 | 격리(isolated) 비중 |
|---|---|
| 0–5% | **21.9%** |
| 5–10% | 10.5% |
| 10–20% | 9.6% |
| 20–50% | 4.7% |
| 50%+ | 1.1% |

근접할수록 격리 비중이 단조 상승한다(전체 평균 4.9%의 4.5배). 교차마진은 청산가가
멀어(중앙 진입가 −38%) 강제청산 대상이 아니고, **사정거리 안의 강제 물량은
격리·고레버리지에서 나온다.** 교차 보유자는 그 움직임을 보고 **재량으로** 손절하며,
그 층이 $\Sigma(u)$ 이고 $L(u)$ 에 전혀 안 잡힌다.

관측 커버리지:
$$\hat L(u) = \phi\,L(u), \qquad \phi \approx 0.05 \;\;(\text{HL 15\%} \times \text{스윕 36\%})$$

---

## 3. 가격 경로 — 외생. 그리고 $D$ 는 상수가 아니다

$$dM \;=\; \kappa\Big(\tfrac{V(u)}{D(u)}\Big)^{b} du \;-\; \lambda\,M\,du,
\qquad b \approx 0.04 \;\;(\text{실측})$$

임계형 $\max(0, V/D-c)^\beta$ 를 **폐기하고** 문턱 없는 오목 멱함수로 되돌린다(§0).
지지집합을 42배 넓혀도 꺾임이 없었다.

$b\approx0.04$ 는 실효적으로 0 이다. 따라서 통상 구간에서

$$dM \approx -\lambda M\,du \qquad\text{(청산항이 사실상 0)}$$

즉 **가격 경로 $S(u)$ 는 청산 분포로 조건부화되지 않는다** — 단, 이것은 **추론이지
직접 검정이 아니다**(아래).

### 3.0 "연료가 얇아지는 곳에서 멈춘다"(v1/H4)의 정확한 지위 — 기각이 아니다

v2 문서는 이 절에서 H4 의 근거가 "제거된다"고 썼다. **과했다.** 근거는 두 개인데
둘 다 간접적이다.

1. **충격 회귀**: 청산이 가격을 거의 안 민다($b\approx0.04$). 밀지 않으면 청산 위치가
   정지점을 정할 이유가 없다 — 그럴듯하지만 **정지점을 직접 잰 것이 아니다.**
2. **H4 해저드 검정**: 연료 3분위별 P(계속 진행). 연료 0 인 관측(24.7%)을 빼면
   thin 0.871 / mid 0.894 / **thick 0.900** 으로 **방향은 예측대로**이고 폭이 3%p 다.
   그런데 이때 쓴 것은 거래량 프로파일로 **복원한** $\hat L$ 이고, 그 복원은 구조적으로
   교락된다(`HAZARD_FINDINGS.md` §3: 복원값이 원본 거래량 프로파일과 거의 구별되지
   않고, 거래량 노드는 정반대 효과를 예측한다).

> **실측 $L(p)$ 로 v1 을 직접 검정한 적은 한 번도 없다.** 대용치로만 해봤고 그게
> 교락됐다. 따라서 v1 은 **불리하지만 미검정**이며, Q2a 로 복권한다(§7).

**두 가설은 정반대 위치를 가리킨다** — 이것이 실무적으로 가장 중요한 구분이다.

| | v1 (정지점) | v2 (체결 품질) |
|---|---|---|
| 논리 | 연료가 **얇아지는** 곳에서 멈춘다 | 연료가 **두꺼운** 곳에서 체결되면 상대가 정보 없는 쪽 |
| 배치 | $\hat L$ 이 **얇은** 자리 | $\hat L$ 이 **두꺼운** 자리 |
| 예측 대상 | 가격 경로 | 체결 품질 |

배타적이지 않다. **두꺼운 덩어리 바로 아래가 얇으면 두 조건이 동시에 만족되며,
이론상 그 자리가 최적이다** — 덩어리가 나를 체결시키고(π 높음) 그 아래가 얇아
더 안 밀린다(정지).

### 3.1 v1 의 올바른 형태 — "연료 두께"가 아니라 **수급 청산**이다

v1 을 "연료가 얇은 곳에서 멈춘다"로 쓴 것은 공급만 본 반쪽이다. 물리적으로 맞는
형태는 호가창을 걸어 내려가는 청산 조건이다:

$$\int_0^{u^*}\Big[\underbrace{D(v)}_{\text{대기 지정매수}} + \underbrace{M(v)}_{\text{도착 시장매수}}\Big]dv
\;=\; \int_0^{u^*} L(v)\,dv \;+\; \text{(외생 매도)}$$

세 항의 상태:

| 항 | 관측 | 문제 |
|---|---|---|
| $D(v)$ 대기 지정매수 | bookDepth (백필 완료) | **그 순간 109~523배 증발**(§3.1 표). 정적 $D$ 는 흡수력을 두 자릿수 과대평가 |
| $M(v)$ 도착 시장매수 | 사후만 (1분봉 taker) | **$D$ 의 20배**(중앙 \$79.7M vs \$3.7M). 경로를 정하는 지배항인데 사전에 안 보임 |
| $L(v)$ 강제매도 | HL $\hat L$, $\phi\approx0.05$ | 교차마진은 캐스케이드 중 청산가가 이동(동시성) |

실증도 불리하다 — 이 회계는 변위가 초과물량/깊이에 거의 비례($b\approx1$)한다고
예측하는데 실측은 $b\approx0.04$, $R^2\approx0.014$ 로 **25배 빗나간다.**
단 $V/D \lesssim 10$ 구간의 측정이고 그 위는 표본이 비어 있어 캐스케이드 영역을
배제하지는 못한다.

**그러나 점추정을 포기하고 해저드로 놓으면 검정 가능해진다.** $M$ 은 관측 불가가
아니라 **조건부 예측 가능**하다 — 지금까지의 변위·변동성 레짐·시간대로 조건부화한
분포를 6년치에서 추정할 수 있다:

$$h(u) \;=\; \mathbb{P}\Big(D(u) + M(u) < L(u) + \text{외생} \;\Big|\; u \text{까지 도달}\Big)$$

### 3.2 Q2a 검정 결과 (2026-08-01) — **실패했고, 실패한 이유가 중요하다**

공급을 $\Delta$OI 로 대용해 HL 없이 검정했다(253이벤트 / 141일 / 2023-01~2026-05,
`analysis/clearing.py`).

**(A) 사후 회계는 부분 지지된다.** 흡수율 = (사전 프로파일의 $X$ 까지 누적 깊이) /
(실제 도착한 순 흐름) 의 **중앙값이 1.47** 이다. 사전에 보이던 호가가 실제 흐름과
같은 자릿수다. 이는 §3.1 의 "순간 깊이 100~500배 증발"과 모순되지 않는다 —
**호가는 먹히는 족족 다시 채워지므로 경로 전체의 누적 흡수량은 순간 깊이와 다르다.**
정적 프로파일은 "순간 흡수력"으로는 못 쓰지만 "누적 흡수량"으로는 쓸 만하다.

**(B) 그러나 사전 예측은 상수를 못 이긴다.** 동일 표본 n=127:

| 예측기 | MAE(%) | R² | Spearman |
|---|---|---|---|
| 상수 | 1.592 | −0.112 | — |
| 변동성 ×6.1 | **1.501** | **+0.050** | 0.270 |
| 흐름만 (깊이 미사용) | 1.666 | −0.150 | −0.106 |
| 회계 (깊이+예상흐름) | 1.603 | **−0.165** | 0.251 |

**무너지는 지점은 $M$ 이다.** 깊이는 사후·사전 모두 흐름 단독보다 낫다
(0.613>0.344, 0.251>−0.106) — 회계 자체는 틀리지 않았다. 사후 0.613 → 사전 0.251 의
낙차 전부가 **도착 유량을 실현값에서 추정값으로 바꾼 비용**이다.

> **v1 이 실패하는 이유는 "청산맵이 정지점을 못 정해서"가 아니라 "지배항인 도착
> 유량을 예보할 수 없어서"다.**

**단, 이 검정이 v1 의 모든 형태를 덮지는 않는다.** 두 가지가 빠졌다.

| | 이 검정(Q2a) | 빠진 형태(Q2c) |
|---|---|---|
| 공급 $L$ | $\Delta$OI **스칼라 하나** | **가격대별 히트맵** ($\hat L(p)$, HL 필요) |
| 시점 | 트리거 시점 **일회 예측** | 가격이 내려가는 동안 **순차 갱신** |

순차 갱신이 결정적이다 — **총 유량을 미리 맞힐 필요가 없다.** 이미 흡수된 양을
관측하면서 다음 구간만 갱신하면 되므로, Q2a 를 죽인 "유량 예보 불가"가 그대로
적용되지 않는다. 다만 넘어가는 부담은 있다: 심볼 내부에서 깊이의 **밴드 구조가
스칼라 하나보다 나을 것이 없었다**(0.610 vs 0.601). 그것은 공급이 스칼라인 상태에서
잰 것이라 짝이 안 맞는 검정이지만, 수요를 가격대별로 쪼개는 것 자체의 값어치를
의심하게 한다. **Q2c 는 Q1 통과 후 별도로 세운다**(§7).

부수: **상수 예측기의 R² 도 −0.112** 다. 낙폭 분포에 레짐 시프트가 있다는 뜻이고,
고정 offset 보정에도 같은 문제가 걸린다.

상세: `analysis/CLEARING_FINDINGS.md`

### 3.1 $D(u,t)$ — 위험한 변수는 분자가 아니라 분모다

$D$ 를 상수로 두면 캐스케이드는 영원히 표본 밖으로 남는다. 실측(`depth_collapse.py`):

(5종 **1,269일**, 2023-01~2026-07, 179만 바. 2023-01 이후 전 일자 백필 완료)

| \|z\| | n | 레짐 대비 붕괴 중앙 | p90 |
|---|---|---|---|
| 0–2 | 1,702,874 | 1.09 | 1.39 |
| 8–12 | 1,004 | 1.64 | 5.19 |
| **20+** | 122 | **6.10** | **36.4** |

2025-10-10 −1% 누적 호가 최저: BTC $2.14M(114×), ETH $0.57M(206×), **SOL $0.07M(561×)**.

주의: 연속 일자를 다 채운 뒤에도 단기(60분) 기준선과 레짐(5일) 기준선이
$|z|>20$ 에서 6.101 vs 6.097 로 사실상 같다. **붕괴가 순간적이라 60분 창도 다 잡는다** —
두 기준선을 나눠 둘 이유가 없다.
그날의 *일중앙* 깊이는 정상이었다 — **붕괴는 지속이 아니라 순간**이며, 하필
$V$ 가 가장 큰 그 몇 분에 아래로 튄다.

이것이 캐스케이드를 만드는 자기여기(self-exciting) 구조다. 분기비를
$R_0 \approx \partial(\text{변위})/\partial V \times \partial L/\partial u$ 로 두면
$\partial(\text{변위})/\partial V \propto 1/D$ 이므로, $R_0$ 를 1 위로 밀어올리는 것은
$V$ 의 증가가 아니라 $D$ 의 붕괴다. **$D$ 는 모델의 상수가 아니라 상태변수다.**

---

## 3.5 채널 A 의 지위 — 게이트가 아니라 형태가 틀렸다

v2 는 v1 의 충격항에 "임계 게이트가 달렸을 뿐 임계 위에서는 살아 있다"고 적었다.
지금은 다르게 말해야 한다. **임계는 없었고, 문턱 없는 오목 효과만 있으며, 그 크기가
경제적으로 무의미하다.**

$$\text{되돌림} \;=\; \underbrace{\text{채널 A: 충격 후 반동}}_{b\,\approx\,0.05,\ \text{실효 0}} \;+\; \underbrace{\text{채널 B: 유동성 공급 프리미엄}}_{\text{상시, 검증됨}}$$

| | 채널 A (v1) | 채널 B (v2 추가) |
|---|---|---|
| 기제 | 강제매도가 호가를 뚫어 과도한 변위 → 반동 | 정보 없는 상대와 체결 → 유동성 프리미엄 |
| 작동 조건 | 문턱 없음 (임계형 반증) | 상시 |
| 관측 상태 | $b\approx0.05$, 큰 사건에서 **부호 미확정** | LIQ−CTRL = **65.7bp** 로 확인 |
| 남은 불확실 | $V$ 대용치의 감쇠편의 | — |
| 대응 문항 | **Q4** (축소됨) | **Q2 · Q3** |

**전략 설계상 함의는 v2 와 같지만 근거가 더 강해졌다**: 채널 B를 기본으로 깔고,
채널 A에는 의존하지 않는다. 다만 채널 A 구간은 **수익이 아니라 위험이 결정되는
구간**이다 — 캐스케이드(되돌림 있음)와 진짜 리프라이싱(되돌림 없음)이 거기서 갈리고,
동시에 깊이가 500배 증발해 체결 자체가 성립하지 않는다(§10).

---

## 4. 채널 B — 체결 상대의 정보성

평상시 구간에서 남는 것은 **누가 내 지정가를 체결시키는가**이다.

가격 $u$ 에 지정매수를 깔면 체결 상대는 정보 있는 매도자와 정보 없는 강제 매도자의
혼합이다. 정보 없는 비중을

$$\boxed{\;\pi(u) \;=\; \frac{U(u)}{U(u) + I(u)}\;}$$

로 두면($I(u)$ = 정보 기반 매도 흐름), 체결 조건부 수익은 그 비중의 증가함수다.

$$\mathbb{E}\big[r \,\big|\, \text{fill}(u)\big] \;=\; r_0 \;+\; \rho\,\pi(u)$$

**실증 근거** — 두 연구가 같은 결론에 독립적으로 도달했다. **v2 문서는 이 둘을
한 표에 섞어 놓아 taker 수치에 maker 체결률이 붙어 있었다. 아래가 정정판이다.**

(1) taker 판 — `event_study_h2.py`, k=8σ, 15분, 비용 10bp, **전량 시장가 진입(체결률 1.00)**

| | n | 평균 | t(NW) | 승률 | **이벤트당** |
|---|---|---|---|---|---|
| LIQ (급변 + OI급감) | 433 | +41.8bp | 2.8 | 61.2% | **+41.8bp** |
| CTRL (같은 크기 급변, OI 유지) | 561 | −23.9bp | −2.5 | 49.7% | −23.9bp |

(2) maker 판 — `maker_1m.py`, 같은 이벤트, offset 2%, 15분, 비용 7bp, 지정가

| | 체결률 | 체결조건부 | t(NW) | 승률 | **이벤트당** |
|---|---|---|---|---|---|
| LIQ | 28.4% | +129.6bp | 2.6 | 70.7% | **+36.8bp** |
| CTRL | 28.9% | +0.1bp | 0.0 | 58.0% | +0.0bp |

체결률이 두 그룹 동일하므로 비교 가능한 표본이다.
**같은 크기의 하락이라도 강제 흐름이 동반된 쪽에서만 되돌린다** — 이는 충격 크기가
아니라 체결 상대의 정보성이 수익을 만든다는 뜻이다(Coval-Stafford 2007, Nagel 2012).

$\rho$ 효과 크기로 인용하는 65.7bp = 41.8 −(−23.9) 는 **(1) taker 판**의 격차다.

### 4.1 식별의 한계 — $\rho$ 는 하한만 안다

$\mathbb{E}[r|\text{fill}] = r_0 + \rho\pi$ 에서 실제로 관측된 것은 $\rho$ 가 아니라
$\rho\,(\pi_{\text{LIQ}}-\pi_{\text{CTRL}}) = 65.7$bp 다. $\pi$ 는 한 번도 관측된 적이
없으므로 $\rho$ 와 $\pi$ 의 스케일은 **분리 식별되지 않는다.**
$\pi_{\text{LIQ}}-\pi_{\text{CTRL}}\le 1$ 이므로 $\rho \ge 65.7$bp (하한).

**현재 결정에는 무해하다** — "LIQ는 거래하고 CTRL은 안 한다"에 필요한 것은 식별되는
차이뿐이다. 분해가 필요해지는 것은 $\pi$ 를 다른 레짐으로 **외삽**할 때다.

---

## 5. 의사결정

$$\boxed{\;\mathrm{EV}(u) \;=\; \underbrace{S(u)}_{\text{체결확률}}\;\Big[\,r_0 + \rho\,\pi(u)\,\Big] \;-\; c_{\text{fee}}\;}$$

- $S(u)$ : 통상 가격 동역학에서 나옴. **$U$ 로 조건부화되지 않는다**(§3)
- $\pi(u)$ : $\hat L(u)$ 로 추정. **여기에만 조건부 정보가 있다**
- 배치: $w(u) \propto \max(\mathrm{EV}(u), 0)$ 사다리
  — 근거가 "분산"이 아니라 **분포 형태 개선**임이 확인됐다(§5.3). Sharpe 는 단일
  레벨과 같지만(0.13) 좌측 꼬리와 승률이 낫다. 목적함수를 평균-분산으로 바꿔도
  배치가 쏠리지 않는다(§5.2).

### 5.1 무차별성 정리 (넘어야 할 기준선)

**"무조건부"의 정확한 뜻**: *배치거리 $u$ 에 대해* 무조건부라는 뜻이며,
**이벤트 선택에는 이미 조건이 걸려 있다** — `stopping_hazard.py` 의 표본은
k=8σ, dOI≤−2% 롱청산 **295건**이다. v2 문서가 이를 "무조건부"라고만 적어
π 조건부 추정치와 비교할 기준선인 것처럼 읽히게 했다.

정보 없이는 $S(u)\,\mathbb{E}[r|u] \approx$ 상수다. 24개 레벨(−0.25%~−6%) 전부에서
**EV 42~52bp(15분, 비용 전)**, $S$ 는 0.79→0.07, $\mathbb{E}[r|u]$ 는
+65→+727bp인데 곱이 평평했다. 두 계열은 정확히 멱함수이고 지수가 소수점 셋째 자리까지
같다 — 이는 발견이 아니라 곱이 평평하다는 것의 항등적 재진술이다:

$$S(u)\approx0.79\Big(\tfrac{u}{0.25\%}\Big)^{-0.763},\qquad
\mathbb{E}[r|u]\approx65\text{bp}\Big(\tfrac{u}{0.25\%}\Big)^{+0.760}$$

$\mathbb{E}[r|u]\propto1/S(u)$ 는 마켓메이커 경쟁의 **무차익 균형조건**이다. 어떤 $u$ 의
EV가 높으면 그쪽에 지정가가 몰려 상쇄되므로, 평평함은 발견이 아니라 균형이 성립한다는
확인이며 그 수준 ~51bp 가 시장이 매기는 유동성 프리미엄의 가격이다.

**기준선 정리 (Q3 가 넘어야 할 값)**

| 출처 | 단위 | 값 |
|---|---|---|
| `stopping_hazard.py` | 이벤트당, 비용 전 | 42~52bp |
| 같음, maker 비용 7bp 차감 | 이벤트당 | 35~45bp |
| `maker_1m.py` offset 2%, 15분 | 이벤트당, 비용 후 | **36.8bp** |
| `event_study_h2.py` taker, 15분 | 이벤트당, 비용 후 | 41.8bp |

셋이 35~45bp 에서 일치한다. **Q3 의 기준선은 "LIQ 이벤트 내, 배치거리 무정보,
비용후 33~38bp/이벤트"** 이며, 진짜 무조건부 대조군은 CTRL 의 **0bp** 다.

$S(u)$ 를 조건부화할 수 없으므로(§3), **무차별성을 깨는 유일한 경로는 $\pi(u)$ 다.**

### 5.2 무차별성은 2차 모멘트에서도 성립한다 (실측 2026-08-01)

체결을 $B\sim\text{Bern}(S)$, 조건부 수익을 평균 $m$·표준편차 $s$ 로 두면

$$\mathrm{Var}(Br) = S s^2 + S(1-S)m^2,\qquad
\text{Sharpe}(u) = \frac{\sqrt{S}\,m}{\sqrt{s^2+(1-S)m^2}}$$

가정하지 않고 $s(u)$ 를 직접 쟀다 (`ladder_risk.py`, 295이벤트 × 40레벨, 15분, 비용 7bp):

| $u$ | $S$ | $m$(bp) | $s$(bp) | $s/m$ | EV(bp) | **sd(bp)** | **Sharpe** |
|---|---|---|---|---|---|---|---|
| 0.25% | 0.790 | 58 | 390 | 6.68 | 46.1 | 348 | 0.133 |
| 1.0% | 0.495 | 79 | 491 | 6.24 | 38.9 | 347 | 0.112 |
| 2.0% | 0.288 | 163 | 640 | 3.94 | 46.8 | 351 | 0.133 |
| 4.0% | 0.122 | 425 | 997 | 2.35 | 51.9 | 375 | 0.138 |
| 6.0% | 0.071 | 720 | 1,296 | 1.80 | 51.2 | 392 | 0.131 |

> **시도당 표준편차도 전 구간 ~350bp 로 평평하다.** $s$ 는 $m$ 에 비례하지도, 상수도
> 아니다($s/m$ 이 6.68→1.80). $S$ 가 떨어지는 만큼 $s$ 가 올라 $\mathrm{Var}$ 의 두
> 항이 정확히 상쇄된다. 결과적으로 **Sharpe ≈ 0.13 이 0.25~6% 전 구간에서 평평**하다.

6% 밖에서는 Sharpe 가 0.094 까지 떨어지고 40레벨 전체 Spearman$(u,\text{Sharpe})=-0.517$
이지만, 그 구간은 $S=0.07$ 이하로 표본이 얇다. **거래 가능한 구간에서 무차별성은
1차뿐 아니라 2차에서도 성립한다** — v2 가 주장한 것보다 오히려 강한 결론이다.
따라서 목적함수를 평균-분산으로 바꿔도 배치가 얕은 쪽으로 쏠리지 않는다.

### 5.3 사다리는 분산되지 않는다 (실측 2026-08-01)

$S(u)$ 는 레벨별 독립 확률이 아니다. $X$ = 지평 내 최대 낙폭이라 하면 체결집합은 항상
$[0,X]$ 이고 $S(u)=\mathbb{P}(X\ge u)$ — 즉 $S$ 는 **$X$ 의 생존함수 하나**이고 체결은
완전 중첩(comonotone)이다. **실측 확인: $\max_u|S_{\text{실측}}(u)-\mathbb{P}(X\ge u)| = 0.0000$.**

$X$ 분위: 25%=0.29%, 50%=0.99%, 75%=2.23%, 90%=4.64%, 99%=19.40%.

| 사다리 | 자본투입률 | 평균(배정bp) | **Sharpe** | 승률 | p05 | 최악 |
|---|---|---|---|---|---|---|
| 균등 40레벨 | 25.1% | 47.0 | **0.13** | 67.1% | −18 | −526 |
| 얕은쪽만(~2%) | 48.7% | 43.4 | 0.13 | 59.7% | −175 | −793 |
| 깊은쪽만(2%~) | 14.2% | 48.7 | 0.14 | 26.4% | 0 | −410 |
| 단일 2.0% | 28.8% | 46.8 | **0.13** | 21.4% | −86 | −711 |

> **사다리는 Sharpe 를 개선하지 못한다** — 레벨을 넓게 깔아도 $X$ 한 변수에 대한
> 노출만 커진다. 다만 좌측 꼬리(p05 −86→−18bp)와 승률(21%→67%)은 개선하므로,
> 사다리의 근거는 "분산"이 아니라 **분포 형태 개선**으로 다시 써야 한다.

#### 숏풋 가설은 확인되지 않았다

| $X$ | n | 자본투입률 | 평균bp | 중앙bp | 최악bp |
|---|---|---|---|---|---|
| [0, 0.5%) | 108 | 1.8% | 2.0 | 0.0 | −0.1 |
| [1, 2%) | 61 | 22.0% | 20.7 | 11.8 | −44 |
| [3, 5%) | 27 | 62.3% | 74.1 | 29.2 | −83 |
| [5, 10%) | 16 | 96.6% | 202.7 | 67.0 | −112 |
| [10%, ∞) | 10 | 100% | **+550.7** | **−7.2** | −526 |

실현손익은 $\int_0^X w(u)(u-v)du = w[X^2/2-vX]$ 로 $X$ 에 2차이고, 되돌림이 없으면
$-wX^2/2$ 인 숏풋이 된다 — **이론상으로는.** 실측은 다르다. **$X$ 가 커질수록 평균이
증가한다**(2.0→550.7bp). 되돌림이 실제로 자주 일어나 $v \ll X$ 가 되기 때문이다.

대신 최심 구간에서 **평균 +550.7bp / 중앙값 −7.2bp** 다. 손실 구조가 아니라
**복권 구조**이며, H2 가 찾은 "손익 63%가 상위 10건"이 여기 국소화된다.
최악 5%(14건)의 기여는 총손익 13,862bp 중 −1,770bp(−13%)로 파괴적이지 않다.

$\pi$ 의 역할은 그대로다 — 이 복권의 **당첨 여부를 사전에 가르는 분류기**다.

---

## 6. 실측된 모수

| 항목 | 값 | 출처 |
|---|---|---|
| 청산액/깊이 (Tardis 실측, **6일**) | 중앙 0.0016, 최대 0.31 | `impact_depth.py` |
| 청산액/깊이 (OI 대용, **1,269일**) | 청산이벤트 중앙 1.42, p99 9.8, 최대 12.9 | `depth_collapse.py` |
| 임계 $c$ | **꺾임의 증거 없음** (단 $V/D\ge10$ 은 n=31 로 미관측) | 같음 |
| 충격 지수 $b$ | ≈ **0.04** (전체, 클러스터 t=8.0). 큰 사건에서 부호 미확정 | 같음 |
| **깊이 붕괴** (\|z\|>20) | 중앙 **6.1배**, p90 **36배** | 같음 |
| **2025-10-10 최저 깊이** | BTC 114× / ETH 206× / SOL **561×** | 같음 |
| **용량** (α=0.10, 이벤트당) | 중앙 **$373K** (BTC $5.0M ~ 알트 $225K), **99% 깊이 구속** | `capacity.py` |
| 용량 가중 − 균등 가중 수익 | −7.9bp, 95% CI **[−75.7, +52.1]** — 0 포함 | 같음 |
| $S(u)$ (LIQ 이벤트 내, 배치거리 무조건부) | 0.79 ($u{=}0.25\%$) → 0.07 ($6\%$) | `stopping_hazard.py` |
| 같은 표본 EV | 42~52bp(비용 전) / 35~45bp(비용 후), 평평 | 같음 |
| $\rho\,(\pi_{\text{LIQ}}{-}\pi_{\text{CTRL}})$ | **65.7bp** (15분, taker) | `event_study_h2.py` |
| 보유지평 (15/60/240분, 이벤트당) | 36.8 / 40.8 / 51.9bp — **60분이 지배당함** | `maker_1m.py` |
| 근접 격리 비중 | 0–5%에서 21.9% (전체 4.9%) | `recheck.py` |
| 지도 안정성 | 40분 프로파일 상관 0.97~1.00 (평온기) | HL 스냅샷 |
| 커버리지 $\phi$ | ≈ 0.05 | OI 실측 대조 |

**미추정**: $\kappa,\ \lambda,\ I(u)$, $\pi$ 의 절대 눈금(따라서 $\rho$ 는 하한만),
$s(u)$(§5.2), $X$ 의 분포(§5.3)

---

## 7. 검정 가능한 예측 (v3)

| # | 예측 | 반증 조건 | 상태 |
|---|---|---|---|
| **Q1** | 실현 청산이 $\hat L$ 두꺼운 가격대에 집중 (대표성) | 상관 0 | 데이터 대기 |
| ~~Q2a~~ | 수급 청산 회계(**공급=$\Delta$OI 스칼라, 일회 예측**)로 $u^*$ 를 상수보다 잘 맞힌다 | 설명력 0 | **실패** — 사전 R²=−0.17 (상수 −0.11, 변동성 +0.05) |
| **Q2c** | **가격대별 $\hat L(p)$ 히트맵을 대기 매수와 밴드별로 대조**하며 가격이 내려가는 동안 **순차 갱신**하면 정지점을 맞힌다 | 갱신이 상수 대비 개선 0 | **미검정.** Q1 통과 후 |
| **Q2b** | $\mathbb{E}[r\mid\text{fill}(u)]$ 가 $\hat L(u)$ 의 증가함수 (v2 체결품질) | $\rho \le 0$ | **핵심** |
| **Q3** | $\pi$ 조건부 EV가 **33~38bp/이벤트**(비용후) 초과 | 초과 못함 | Q2 통과 시 |
| Q4 | 극단 $V/D$ 에서 충격항이 살아남 | 임계 없음·$b\approx0$ | **부분 검정 — 임계형 반증** |
| Q5 | 위험조정수익 $\text{Sharpe}(u)$ 도 평평한가 | 얕은 쪽으로 기울면 배치 재설계 | **통과** — 0.25~6%에서 0.13 평평(§5.2) |
| Q6 | 사다리 손익의 $X$ 분포가 숏풋인가 | 좌측꼬리가 프리미엄을 먹음 | **반증** — $X$ 커질수록 평균 증가(§5.3) |

**Q2 는 반드시 *같은 $u$ 안에서* 비교해야 한다.** 레벨 간 비교로 짜면 관측 커버리지
$\phi(u)$ 의 변위 의존성(§8.2)과 혼동된다 — §2 의 격리 비중 21.9%→1.1% 단조 변화가
바로 $\partial\phi/\partial u\ne0$ 의 정황이다. 같은 $u$ 안에서 "$\hat L$ 두꺼운 이벤트
vs 얇은 이벤트"로 비교하면 $\phi(u)$ 가 상쇄되고, $\phi$ 의 형태를 먼저 추정할 필요가 없다.

**Q2가 v1과 결정적으로 다르다.** v1은 "$L$ 이 두꺼우면 더 멀리 간다"(경로 예측)였고,
v2는 "$L$ 이 두꺼운 곳에서 체결되면 상대가 정보 없는 쪽일 확률이 높다"(체결 품질)이다.
전자는 §0·§3에서 형태까지 반증됐고, 후자는 §4의 LIQ/CTRL 격차가 이미 간접 지지한다.

---

## 8. 알려진 식별 문제

1. **$\Sigma(u)$ 관측 불가.** 재량 손절 층은 어디에도 기록되지 않는다. $\hat L$ 을
   $U$ 의 대용으로 쓰는 것은 가정이며, 근접 구간에서 격리 비중이 21.9%뿐이라는 것은
   나머지 78%가 교차(= 강제청산 임박이 아닌) 물량임을 뜻한다.
2. **$\phi$ 의 변위 의존성.** HL 사용자가 시장 전체와 레버리지 구성이 다르면 $\hat L$ 의
   형태 자체가 왜곡된다. HL 포지션 97%가 교차마진인데 CEX 리테일은 격리·고레버리지
   비중이 높다 — **미검증 외삽**.
3. **동시성.** 청산은 가격 움직임의 결과다(§0). 예측변수는 변위 측정 구간 *이전*에
   관측된 것만 쓴다. 코드에서 강제하고 있다.
4. **$V$ 대용치의 감쇠편의.** §0 의 확장 검정은 OI 급감액을 $V$ 로 썼고, 여기에는
   자발적 청산이 섞여 있다. 측정오차는 회귀계수를 0 쪽으로 밀므로 $b\approx0.05$ 는
   과소추정일 수 있다. 진짜 $V$(Tardis 유료)로 재확인해야 확정된다.
5. **거래소 피드 편향.** Binance forceOrder는 심볼당 초당 1건에 걸려 있고 누락이
   강도 의존적이다(Bybit 대비 88.7× → 0.48×, 185배 변동). 물량은 Bybit 전건만 쓴다.
6. **표본 일수 확인 습관.** §0 의 사고는 "1,423버스트"라는 큰 n 이 6일에서 나왔다는
   것을 확인하지 않아 생겼다. 이후 모든 결과에 **관측 수와 일수를 함께** 적는다.

---

## 9. 다음 단계

1. $\hat L(u)$ 스냅샷 축적 (진행 중: 4000계좌/15분 + 근접 60초)
2. **Q1 대표성** → 통과 시 Q2로
3. **Q2**: 같은 $u$ 안에서 $\hat L$ 두꺼운/얇은 이벤트를 비교해 $\rho$ 추정
4. Q3: $\pi$ 조건부 EV vs 기준선 **33~38bp/이벤트(비용후)**
5. Q5(§5.2) · Q6(§5.3) — 기존 실행 재사용, 새 데이터 불필요
6. Q4 확정은 Tardis 유료(대형 이벤트 포함) 확보 후

---

## 10. 비용과 용량 — α 가정 없이 (실측 2026-08-01)

### 10.1 수수료는 맞았고, 슬리피지가 빠져 있었다

`COST_BPS = 7.0` 은 Binance USD-M 선물 수수료다(maker 진입 0.02% + taker 청산 0.05%).
이 값은 정확하다. 빠진 것은 **청산 슬리피지**다 — 진입은 우리가 호가라 0 이지만
HOLD 후 시장가로 나갈 때 호가창을 걸어 내려간다. H2 §5 가 "비용을 상수로 가정해
낙관 방향으로 편향"이라 적어 둔 그 항이다.

누적 깊이가 국소적으로 $D(u)=Au^{\beta}$ 이면 크기 $Q$ 의 명목가 가중 평균 이탈은

$$\overline{\mathrm{slip}}(Q) \;=\; u^{*}(Q)\cdot\frac{\beta}{\beta+1},
\qquad D(u^{*}) = Q$$

**실측 — 슬리피지가 수수료를 넘는 지점은 약 \$500K** (5종 73건):

| 크기 | \$100K | \$500K | \$1M | \$2.5M | \$5M |
|---|---|---|---|---|---|
| slip 중앙 | 1.3bp | **6.4bp** | 12.7bp | 31.8bp | 64.5bp |
| 수수료(7bp) 대비 | 0.2배 | **0.9배** | 1.8배 | 4.5배 | 9배 |

### 10.2 크기별 순EV — α 를 대체한다

$$\mathrm{EV}_{\mathrm{net}}(Q) = S \times \big[\,\mathbb{E}[r|\text{fill}] - c_{\text{fee}} - \overline{\mathrm{slip}}(Q)\,\big]$$

| 크기 | 순(체결당) | 순(이벤트당) | 달러/이벤트 |
|---|---|---|---|
| \$100K | 81.5bp | 23.2bp | +\$232 |
| \$1M | 70.1bp | 19.9bp | +\$1,991 |
| **\$2.5M** | **51.0bp** | 14.5bp | **+\$3,624** |
| \$5M | 18.3bp | 5.2bp | +\$2,599 |
| \$10M | **−61.8bp** | −17.5bp | −\$17,545 |

> **달러 손익 최적 크기 \$2.5M/이벤트, 손익분기 \$5~10M.**
> 이전 판의 $\alpha=0.10$ 기반 \$373K 는 **7배 보수적**이었다. 다만 \$2.5M 에서는
> 체결당 수익을 82.7 → 51.0bp 로 38% 내주고 크기를 250배 키우는 거래다.

**심볼 편차 20배** — \$1M 청산 슬리피지가 BTC 0.9bp / ETH 1.7bp / 알트 20~22bp.
**심볼별 균등 배분은 명백히 틀렸다.**

### 10.3 유량은 구속하지 않는다

지정매수는 테이커 매도가 와야만 체결되므로 유량 $F$ 가 진입 상한이지만, 실측에서
**99% 의 사건에서 청산 깊이가 먼저 구속한다**(유량 중앙 \$79.7M vs 깊이 중앙 \$3.7M).
문제는 "체결이 되느냐"가 아니라 **"나올 수 있느냐"** 다.

### 10.4 세 가지 하방 편의 — 실제로는 이보다 나쁘다

1. **최악의 날이 표본에 없다.** 2025-10-10 은 깊이 피드가 얼어 빠졌다(§0 벤더 결함).
2. **1% 안쪽이 외삽이다.** 균일 밀도 가정이 깊이를 1.20배 과대평가 → slip 과소추정.
3. **정적 호가 가정.** \$2.5M 을 \$3.7M 호가에 던지면 남들도 뺀다. 하한이다.

상세: `analysis/SLIPPAGE_FINDINGS.md`, `analysis/CAPACITY_FINDINGS.md`

**"큰 수익 사건일수록 용량이 작다"는 확인되지 않았다.** 용량 가중 − 균등 가중 =
−7.9bp, 일자 클러스터 부트스트랩 95% CI [−75.7, +52.1] 로 0 을 포함한다.
(이 문서 초판은 −76.2bp 로 적었으나, 그것은 2025-10-10 DOGE 한 건이 만든 값이고
그 사건의 $D_{exit}$ 가 고정 피드 구간이었다. 철회한다.)

**가장 큰 미지수**: 2025-10-10 의 두 체결 이벤트 모두 청산 시각 ±2분에 쓸 수 있는
깊이 스냅샷이 없다 — 고정 필드 필터가 걸러낸 구간이 정확히 캐스케이드 구간이다.
**캐스케이드 당일의 용량은 현재 데이터로 측정 불가**다.

라이브 축적: `paper/baseline.py` 가 체결 시점 유량과 청산 시점 깊이를 직접 조회해
기록한다. 단 REST limit=1000 도달 한계로 종목마다 밴드가 다르다(BTC 0.1%, ETH 0.5%,
알트 2%) — 위 −1% 수치와 직접 비교하면 안 된다.

상세: `analysis/CAPACITY_FINDINGS.md`
